# อัตราการเกิดปฏิกิริยาเคมี — แผนมโนทัศน์ทั้งชุด (6 มโนทัศน์)

เอกสารส่งต่องาน · เคมี ม.5 · บทที่ 8 ตามหนังสือเรียน สสวท.
อัปเดตล่าสุด: 2 กันยายน 2569

repo: https://github.com/Nonsea-teacher/Chemical-reaction-rate
เว็บ: https://nonsea-teacher.github.io/Chemical-reaction-rate/

---

## สถานะปัจจุบัน

| # | มโนทัศน์ | ไฟล์ | สถานะ |
|---|---|---|---|
| 01 | อัตราการเกิดปฏิกิริยาคืออะไร | `01-rate-definition.html` | ✅ เสร็จ |
| 02 | ทฤษฎีการชน (รวม Eₐ ไปกลับ/ΔE, กราฟหลายยอด, ตัวเร่งปฏิกิริยา — เดิมคือมโนทัศน์ 2+3) | `02-collision-theory.html` | ✅ เสร็จ |
| 03 | ปัจจัยที่มีผลต่ออัตราการเกิดปฏิกิริยา | `03-factors.html` | ✅ เสร็จ |
| 04 | กฎอัตราและอันดับของปฏิกิริยา | `04-rate-law.html` | ⬜ ยังไม่เริ่ม |
| 05 | กลไกของปฏิกิริยา | `05-mechanism.html` | ⬜ ยังไม่เริ่ม |
| 06 | การเชื่อมโยงและการประยุกต์ | `06-applications.html` | ⬜ ยังไม่เริ่ม |

> **หมายเหตุการรวมมโนทัศน์:** มโนทัศน์ที่ 3 เดิม (แผนภาพพลังงานของปฏิกิริยา: Eₐ ไปกลับ, กราฟหลายยอด/ขั้นกำหนดอัตรา, ตัวเร่งปฏิกิริยา) ถูกยุบรวมเข้าไปในมโนทัศน์ 2 ทั้งหมดแล้ว (ดูหัวข้อ 2.3–2.5 ในไฟล์ `02-collision-theory.html`) เพื่อความกระชับ ชุดจึงเหลือ 6 มโนทัศน์ และเลขไฟล์ 4–7 เดิมขยับลงมาเป็น 3–6

`index.html` = สารบัญเชื่อมทุกหน้า — **ทำหน้าใหม่เสร็จแล้วอย่าลืมมาแก้ลิงก์ในนี้ด้วย**
(เปลี่ยน `<a class="soon">` เป็น `<a href="0X-....html">` และแก้ตัวเลข "X / 6 พร้อมใช้" ที่หัวหน้า)

---

## บริบทของงาน (สำคัญ ต้องอ่านก่อนทำต่อ)

- **ผู้ใช้หลักคือครู** ที่ยืนสอนหน้าห้องและฉายหน้านี้ขึ้นโปรเจกเตอร์ ไม่ใช่นักเรียนอ่านเอง
  → ตัวหนังสือใหญ่ คอนทราสต์สูง อ่านจากหลังห้องได้ พื้นสว่างเพื่อสู้แสงในห้องที่เปิดไฟ
- **นักเรียนเปิดซ้ำในมือถือทีหลังได้** → ต้องไม่พังบนจอแคบ
- **โหมดสว่างอย่างเดียว** ไม่ทำ dark mode (ผู้ใช้สั่งไว้ชัดเจน)
- **หนึ่งมโนทัศน์ = หนึ่งไฟล์ HTML ที่ยืนได้ด้วยตัวเอง** ไม่มี build step ไม่มี dependency ภายนอก
  (ยกเว้น Google Fonts ซึ่งเป็นโฮสต์เดียวที่ CSP ของ Artifact ยอมให้โหลด)

---

## ⚠️ เรื่องที่ต้องรู้ก่อนไปทำต่อเครื่องอื่น

1. **โฟลเดอร์ `สสวท book/` ไม่ได้อยู่ใน git** (ใส่ `.gitignore` ไว้ เพราะเป็นสแกนหนังสือมีลิขสิทธิ์ และ repo นี้เป็น public)
   → **ต้องก๊อปโฟลเดอร์นี้ไปเครื่องใหม่ด้วยมือ** (USB / cloud drive) ไม่งั้นจะไม่มีต้นฉบับโจทย์และเนื้อหาให้อ้างอิง
   ข้างในมี: คู่มือครู เคมี ม.5 เล่ม 3 (ฉบับปรับปรุง พ.ค. 66) PDF + ภาพถ่ายหน้าหนังสือ 19 รูป (หน้า 54–120)

2. **ไฟล์ `01-rate-definition.html` ใหญ่ ~2.9 MB** เพราะฝังภาพ 8 ภาพเป็น base64
   → เครื่องมือพรีวิวบางตัวเปิดไม่ไหว ถ้าจะดูให้เปิดผ่าน GitHub Pages หรือเปิดไฟล์ในเบราว์เซอร์ตรง ๆ

3. **ไฟล์ `img-*.png` ต้นฉบับยังเก็บไว้ใน repo** ถึงแม้จะฝัง base64 ในหน้าเว็บแล้ว
   → เก็บไว้เผื่อแก้/ครอปใหม่ ไม่ต้องแกะกลับจาก base64

---

## มโนทัศน์ที่ 1 — อัตราการเกิดปฏิกิริยาคืออะไร ✅

**ไฟล์:** `01-rate-definition.html` · **Artifact:** https://claude.ai/code/artifact/1949a00f-edd9-4453-932c-045f34b1f447

### สิ่งที่นักเรียนต้องทำได้
1. เขียนนิยาม `Δ[สาร]/Δt` ระบุหน่วย และอธิบายว่าทำไมสารตั้งต้นต้องมีเครื่องหมายลบ
2. คำนวณอัตราเฉลี่ยจากตารางข้อมูล
3. แยกอัตราเฉลี่ย / อัตรา ณ ขณะหนึ่ง / อัตราเริ่มต้น และอ่านจากกราฟได้
4. แปลงอัตราของสารหนึ่งเป็นอีกสารหนึ่งด้วยเลขสัมประสิทธิ์
5. เลือกวิธีวัดอัตราที่เหมาะกับปฏิกิริยาที่กำหนด

### โครงหน้าที่ทำไปแล้ว
- เกริ่นนำ 3 ภาพ (ดินปืน / สนิม / หินงอกหินย้อย) + คำถามเปิด
- **1.1** นิยามและหน่วย + ภาพชุดเก็บแก๊ส + ตารางหน่วย 4 แบบ
- **1.2** อัตราเฉลี่ยคำนวณด้วยมือ + **ตัวอย่างที่ 1** (ตาราง 8.1 NO₂→NO+O₂ ข้อมูลจริง) + ตรวจสอบความเข้าใจ 2 ข้อ + **ตัวอย่างที่ 2** (เซลล์เชื้อเพลิง H₂+O₂)
- **1.3** เฉลี่ย/ขณะหนึ่ง/เริ่มต้น + **แผงกราฟโต้ตอบ (ลากเส้นตัด→เส้นสัมผัส)** + วิธีลากเส้นสัมผัสด้วยมือ 4 ขั้น + แบบฝึกเลือกเส้นสัมผัสที่ถูก + ตรวจสอบความเข้าใจ 2 ข้อ (Zn+HCl จากหนังสือ)
- **1.4** สัมประสิทธิ์ + กราฟ N₂/H₂/NH₃ ความชัน 1:3:2 + คำถาม 3 ข้อ (สมการยังไม่ดุล ให้นักเรียนดุลเอง)
- **1.5** การ์ดวิธีวัด 5 แบบ พร้อมภาพประกอบทุกใบ
- **1.6** กับดัก 4 ข้อ (❌→✅)
- **1.7** แบบฝึกหัด 8.1 ฉบับเต็มจากคู่มือครู 4 ข้อ พร้อมเฉลยละเอียด

### บทเรียนจากการทำหน้านี้ (เอาไปใช้กับหน้าอื่น)
- **ลำดับสำคัญมาก**: ให้นักเรียน**คำนวณด้วยมือก่อน** (1.2) แล้วค่อยเปิดกราฟโต้ตอบ (1.3) เป็นบทสรุปเชิงภาพ — ตอนแรกวางกราฟไว้บนสุดแล้วผู้ใช้บอกว่าเข้าใจยาก
- **ต้องมีภาพ** อย่าให้ทั้งหน้ามีแต่ตัวหนังสือกับกราฟ SVG
- **หน่วยเขียนเป็นเศษส่วนซ้อน** (`.frac`) ไม่ใช่ `cm³·s⁻¹` — นักเรียนมองยาก

---

## มโนทัศน์ที่ 2 — ทฤษฎีการชน ✅

**ไฟล์:** `02-collision-theory.html` · **Artifact:** https://claude.ai/code/artifact/73c90ec5-48cf-4523-8fe8-8b1f44d94c6b

### สิ่งที่นักเรียนต้องทำได้
1. บอกเงื่อนไข 2 ข้อของการชนที่ทำให้เกิดปฏิกิริยา (พลังงาน ≥ Eₐ + ทิศทางถูกต้อง)
2. อธิบายความหมายของ Eₐ และสารเชิงซ้อนกัมมันต์
3. อธิบายว่าอุณหภูมิเปลี่ยนอัตราได้อย่างไร โดยไม่เปลี่ยน Eₐ
4. ใช้ทฤษฎีการชนอธิบายว่าทำไมความเข้มข้น/พื้นที่ผิวจึงมีผล

### โครงหน้าที่ทำไปแล้ว
- เกริ่นนำ 2 ภาพเทียบ (ชนถูกด้าน→เกิดปฏิกิริยา / ชนเฉียง→สะท้อนแยก)
- **2.1** เงื่อนไข 2 ข้อ + ปฏิกิริยา NO+NO₃→2NO₂ (ตัวอย่างเรื่องทิศทางจากหนังสือ หน้า 73–75)
- **2.2** แผนภาพเนินพลังงาน (Eₐ / สารเชิงซ้อนกัมมันต์ / ΔH) เป็น SVG
- **2.3 (จุดเด่น)** กราฟการกระจายพลังงานจลน์ **ลากอุณหภูมิได้** พื้นที่แรเงาโตจาก 5.3% → 27.8% เมื่อลาก T 30→55
- **2.3** Eₐ ไปข้างหน้า/ย้อนกลับ (Eₐ' = Eₐ − ΔE) + กราฟตัวอย่างตัวเลข + กราฟย่อยคาย/ดูดพลังงาน (เดิมคือเนื้อหามโนทัศน์ 3)
- **2.4** ปฏิกิริยาหลายขั้นตอน — ขั้นกำหนดอัตรา + สารมัธยันตร์ (กราฟสองยอด, เดิมคือเนื้อหามโนทัศน์ 3)
- **2.5** ตัวเร่งปฏิกิริยา — ลด Eₐ แต่ไม่แตะ ΔE (ปุ่มสลับมีตัวเร่ง/ไม่มี, เดิมคือเนื้อหามโนทัศน์ 3)
- **แบบฝึกหัดท้ายบท** — แบบฝึกหัด 8.2 จากหนังสือครบ 4 ข้อ (พร้อมเฉลยกางได้)

> **มโนทัศน์ที่ 3 เดิม (แผนภาพพลังงานของปฏิกิริยา) ถูกยุบรวมเข้าไปในมโนทัศน์ 2 ข้างต้นทั้งหมดแล้ว** — ไม่ต้องทำไฟล์ `03-energy-diagram.html` แยกอีก เนื้อหาเดิม (Eₐ ไปกลับ, ΔH/ΔE, กราฟหลายยอด/ขั้นกำหนดอัตรา/สารมัธยันตร์, ตัวเร่งปฏิกิริยา) อยู่ในหัวข้อ 2.3–2.5 ของไฟล์ `02-collision-theory.html` ครบแล้ว

---

## มโนทัศน์ที่ 3 — ปัจจัยที่มีผลต่ออัตราการเกิดปฏิกิริยา ⬜

**ไฟล์ที่จะทำ:** `03-factors.html` · อ้างอิงหนังสือหน้า 80–87 (กิจกรรม 8.4, 8.5, 8.6)

### สิ่งที่นักเรียนต้องทำได้
อธิบายผลของแต่ละปัจจัย**ด้วยทฤษฎีการชน** ไม่ใช่ท่องจำ:

| ปัจจัย | อธิบายด้วย | จุดที่ข้อสอบเจาะ |
|---|---|---|
| ธรรมชาติของสาร | ชนิดพันธะ/สถานะ | ไอออนเร็วกว่าโคเวเลนต์ |
| ความเข้มข้น | ความถี่การชนเพิ่ม (Eₐ ไม่เปลี่ยน) | เพิ่มความเข้มข้นแล้วอัตราเพิ่มกี่เท่า |
| ความดัน/ปริมาตร (แก๊ส) | เทียบเท่าเพิ่มความเข้มข้น | ลดปริมาตร ≠ เติมแก๊สเฉื่อยที่ปริมาตรคงที่ |
| พื้นที่ผิว | ของแข็งบดละเอียด → ผิวสัมผัสมาก | ก้อน vs ผง กราฟชันต่างกันแต่**ปริมาตรแก๊สสุดท้ายเท่ากัน** |
| อุณหภูมิ | เพิ่มสัดส่วนอนุภาคที่มี E ≥ Eₐ → **ค่า k เพิ่ม** | เป็นปัจจัยเดียวในกลุ่มนี้ที่เปลี่ยน k |
| ตัวเร่งปฏิกิริยา | สร้างเส้นทางใหม่ที่ Eₐ ต่ำกว่า | ไม่เปลี่ยน ΔH, เร่งทั้งไปหน้าและย้อนกลับ |

### แนวคิดการออกแบบหน้า
- **จุดเด่นที่ควรทำ**: กราฟปริมาตรแก๊ส–เวลาที่มี**หลายเส้นทับกัน** พร้อมปุ่มสลับปัจจัย — เน้นให้เห็นว่าเส้นชันต่างกันแต่**ไปจบที่ระดับเดียวกัน** (ยกเว้นตอนเปลี่ยนปริมาณสาร) นี่คือมโนทัศน์ที่นักเรียนพลาดบ่อยที่สุด
- ข้อมูลจริงจากกิจกรรม 8.4 (CaCO₃ ก้อน/ผง + HCl 0.2/0.5 M, 4 บีกเกอร์) หน้า 80–81 → ทำเป็นตารางเทียบได้เลย
- กิจกรรม 8.5 (H₂O₂ + KI เป็นตัวเร่ง) หน้า 84–85 → ใช้สอนตัวเร่ง
- ควรมีภาพประกอบ: ก้อนหินปูน vs ผงหินปูน / ชุดทดลอง 4 บีกเกอร์
- ต่อยอดจากรูป 8.8, 8.9 ในหนังสือ (แผนภาพอนุภาคแสดงความเข้มข้นและพื้นที่ผิว)

### กับดักที่ต้องใส่
- ❌ ตัวเร่งทำให้ได้ผลิตภัณฑ์มากขึ้น → ✅ ได้เท่าเดิม แค่ถึงเร็วขึ้น
- ❌ เพิ่มอุณหภูมิทำให้ Eₐ ลดลง → ✅ Eₐ คงที่ (ย้ำจากมโนทัศน์ 2)
- ❌ บดผงแล้วได้แก๊สมากขึ้น → ✅ ได้เท่าเดิม แค่เร็วขึ้น

---

## มโนทัศน์ที่ 4 — กฎอัตราและอันดับของปฏิกิริยา ⬜

**ไฟล์ที่จะทำ:** `04-rate-law.html` · อ้างอิงหนังสือหน้า 88–89 (แบบฝึกหัด 8.3)

> **หมายเหตุ:** นี่คือหัวข้อที่มีน้ำหนักมากที่สุดในข้อสอบเข้ามหาวิทยาลัย ควรลงแรงมากที่สุด

### สิ่งที่นักเรียนต้องทำได้
1. เขียน `rate = k[A]^m[B]^n` และรู้ว่า **m, n หาจากการทดลองเท่านั้น ไม่ใช่จากเลขสัมประสิทธิ์** ← หัวใจ
2. หาอันดับด้วย**วิธีอัตราเริ่มต้น** (คุมตัวแปรอื่นคงที่ เทียบทีละคู่)
3. หาอันดับรวม m + n และเข้าใจความหมายของอันดับศูนย์
4. **หาค่า k พร้อมหน่วย** — หน่วยเปลี่ยนตามอันดับรวม: `(mol·dm⁻³)^(1−รวม)·s⁻¹` (เด็กตอบหน่วยผิดบ่อย)
5. รู้ว่า k ขึ้นกับอุณหภูมิและตัวเร่งเท่านั้น ไม่ขึ้นกับความเข้มข้น

### แนวคิดการออกแบบหน้า
- **จุดเด่นที่ควรทำ**: ตารางการทดลองแบบ**โต้ตอบ** — คลิกเลือกสองแถวที่จะเทียบ แล้วระบบไฮไลต์ว่าตัวแปรไหนคงที่/ตัวไหนเปลี่ยน พร้อมโชว์อัตราส่วนที่คำนวณได้ → ฝึกทักษะ "เลือกคู่ที่ถูก" ซึ่งเป็นจุดที่นักเรียนพลาด
- ข้อมูลจริงจากแบบฝึกหัด 8.3 ข้อ 2 (หน้า 88): สาร A, B → A₂B มี 4 การทดลอง
  | การทดลอง | A เริ่มต้น (M) | B เริ่มต้น (M) | A₂B ที่ 10 นาที (M) |
  |---|---|---|---|
  | 1 | 1.00 | 0.50 | 0.40 |
  | 2 | 1.00 | 1.00 | 0.40 |
  | 3 | 1.00 | 1.50 | 0.40 |
  | 4 | 0.50 | 0.50 | 0.20 |
- ควรมีเครื่องคำนวณหน่วยของ k ที่อัปเดตตามอันดับรวมที่เลือก
- รูปแบบข้อสอบมาตรฐาน: ตาราง 3–4 แถว → หาอันดับ → เขียนกฎอัตรา → หา k → ทำนายอัตราการทดลองที่ 5 (ครบวงจรในข้อเดียว)

---

## มโนทัศน์ที่ 5 — กลไกของปฏิกิริยา ⬜

**ไฟล์ที่จะทำ:** `05-mechanism.html`

### สิ่งที่นักเรียนต้องทำได้
1. เข้าใจว่าปฏิกิริยาส่วนใหญ่เกิดหลายขั้น ผลรวมทุกขั้น = สมการรวม
2. ระบุ**ขั้นกำหนดอัตรา** (ขั้นที่ช้าที่สุด / Eₐ สูงสุด) และรู้ว่ากฎอัตราสอดคล้องกับสารตั้งต้นของขั้นนี้เท่านั้น
3. ใช้กฎอัตราจากการทดลองมา**ตรวจสอบว่ากลไกที่เสนอสมเหตุสมผลหรือไม่**
4. แยก **โมเลกุลาริตี** (จากกลไก) ออกจาก **อันดับ** (จากการทดลอง)
5. แยก **สารมัธยันตร์** (เกิดขั้นแรก ใช้หมดขั้นหลัง) จาก **ตัวเร่ง** (เข้าขั้นแรก ออกขั้นหลัง)

### แนวคิดการออกแบบหน้า
- **จุดเด่นที่ควรทำ**: แผนภาพพลังงานหลายยอดที่**คลิกแต่ละขั้นได้** แล้วโชว์สมการขั้นย่อยนั้น + ไฮไลต์ว่าขั้นไหนคือคอขวด
- อุปมา "คอขวด" / "ทางด่วนช่องเดียว" — เร่งขั้นที่เร็วอยู่แล้วไม่ช่วยอะไร
- ตัวอย่าง NO + NO₃ → 2NO₂ (ต่อยอดจากมโนทัศน์ 2 ที่ใช้ปฏิกิริยานี้แล้ว) หรือ N₂O₅
- แบบฝึกหัด: ให้กลไก 2 แบบ + กฎอัตราจากการทดลอง แล้วถามว่ากลไกไหนสอดคล้อง

> นี่คือมโนทัศน์ที่ยากที่สุดและแยกเด็กคะแนนสูงออกจากเด็กท่องจำ

---

## มโนทัศน์ที่ 6 — การเชื่อมโยงและการประยุกต์ ⬜

**ไฟล์ที่จะทำ:** `06-applications.html` · อ้างอิงหนังสือหน้า 90 (สรุปแนวคิดสำคัญ) และหน้า 54 (แอร์แบ็ก NaN₃)

### สิ่งที่นักเรียนต้องทำได้
1. **อัตรา ≠ สมดุล**: ตัวเร่งเปลี่ยนอัตราแต่ไม่เปลี่ยน K / อุณหภูมิเปลี่ยนทั้งอัตราและ K / ปฏิกิริยาที่เอื้อทางพลังงานอาจเกิดช้ามากถ้า Eₐ สูง (เพชร → แกรไฟต์)
2. เชื่อมกับปริมาณสัมพันธ์: สารกำหนดปริมาณคุม "ปริมาณผลิตภัณฑ์สุดท้าย" ส่วนปัจจัยด้านอัตราคุม "ความชันของกราฟ"
3. อธิบายการประยุกต์ในชีวิตจริงด้วยหลักการจากบทนี้

### แนวคิดการออกแบบหน้า
- **จุดเด่นที่ควรทำ**: ผังสรุปทั้ง 6 มโนทัศน์เชื่อมโยงกัน (concept map) เป็น SVG — ใช้เป็นหน้าทบทวนก่อนสอบได้
- กรณีศึกษา: แอร์แบ็ก (NaN₃ ต้องเร็วมาก — หนังสือใช้เปิดบท), การถนอมอาหาร/ตู้เย็น, เครื่องฟอกไอเสียรถยนต์, กระบวนการฮาเบอร์, เอนไซม์ในร่างกาย, สารกันหืน
- ข้อสอบแนวสถานการณ์ (context-based) ใช้ส่วนนี้เยอะขึ้นเรื่อย ๆ
- ควรมีตารางสรุป "ปัจจัยไหนเปลี่ยน k / ปัจจัยไหนเปลี่ยน K / ปัจจัยไหนเปลี่ยนทั้งคู่"

---

# คู่มือทางเทคนิค (ทำหน้าใหม่ให้เหมือนของเดิม)

## วิธีเริ่มหน้าใหม่ที่เร็วที่สุด
ก๊อป `02-collision-theory.html` เป็นไฟล์ใหม่ แล้วแก้เนื้อหา — ไฟล์นี้มี CSS ครบทุกคอมโพเนนต์และเล็กกว่าไฟล์ 01 มาก (ไม่มีภาพ base64 เยอะ)

## Design tokens (ห้ามเปลี่ยน — ใช้ร่วมกันทั้งชุด)

```css
--ground:#D9DEDA;   /* พื้นหลังนอกแผง */
--panel:#F7F8F6;    /* พื้นแผงอีนาเมล */
--panel-2:#ECEFEB;  /* พื้นรอง */
--ink:#171D1B;      /* หมึกหลัก */
--ink-2:#5A6461;    /* หมึกรอง */
--ink-3:#8B948F;    /* ป้ายกำกับจาง */
--rule:#BAC2BD;     /* เส้นขอบ */
--rule-2:#D5DBD6;   /* เส้นกริด */
--index:#C0301A;    /* สีชาด = เน้น/เส้นสัมผัส/ผิด */
--index-soft:#F2D9D3;
--cobalt:#17457F;   /* น้ำเงิน = ข้อมูล/เส้นตัด/ถูก */
--cobalt-soft:#D6E0EE;
--sans:"Anuphan";      /* เนื้อความ */
--tech:"Bai Jamjuree"; /* หัวเรื่อง ตัวเลข ป้ายกำกับ */
```

**ความหมายของสี (ใช้ให้สม่ำเสมอ):**
- **น้ำเงินโคบอลต์** = ค่าที่วัดได้ / เส้นตัด / คำตอบถูก / กล่องอ้างอิงหนังสือ
- **สีชาด** = ค่าที่เน้น / เส้นสัมผัส / คำตอบผิด / เลขหัวข้อ

## คลาส CSS ที่มีให้ใช้แล้ว

| คลาส | ใช้ทำอะไร |
|---|---|
| `.plate` | แผ่นป้ายชื่อหัวหน้า (หน่วยการเรียนรู้ / เลขมโนทัศน์ / ปฏิกิริยา) |
| `.panel` + `.face` + `.readout` + `.win` | แผงหน้าปัด: กราฟซ้าย + หน้าต่างอ่านค่าขวา |
| `.caliper` | แถบควบคุมใต้แผง (สไลเดอร์ + ปุ่ม) |
| `.sec` + `.sec-hd` + `.sec-no` + `.sec-bd` | กล่องหัวข้อหลัก (2.1, 2.2, ...) |
| `.notes` | กริดกล่องนิยามสั้น ๆ (`<b class="k">หัวข้อ</b>เนื้อหา`) |
| `.eq` | กล่องสมการกลางหน้า (เพิ่ม `.long` ถ้าสมการยาว) |
| `.frac` | เศษส่วนซ้อน — `<span class="frac"><i class="n">mol</i><i class="d">dm³·s</i></span>` |
| `.cards` | การ์ดเรียงกริด (`<article>` ข้างใน) |
| `.traps` | กล่องเทียบ ❌ เข้าใจผิด / ✅ ที่ถูกต้อง |
| `.quiz` | คำถามปรนัยตอบทันที (`data-a="index คำตอบถูก"`) |
| `.cite` + `.cite-hd` | กล่องน้ำเงิน = อ้างอิงจากหนังสือเรียนจริง |
| `.checkhd` + `.check` | กล่อง "ตรวจสอบความเข้าใจ" ระหว่างเรียน |
| `.ex` + `.ex-body` + `.ans` + `.work` | แบบฝึกหัดท้ายบท + เฉลยแบบพับได้ (`<details>`) |
| `ol.steps` | ขั้นตอนมีเลขกำกับตัวใหญ่สีชาด (**อย่าพิมพ์ "1." ซ้ำในข้อความ** เลขขึ้นอัตโนมัติ) |
| `.hookrow` | แถวภาพเกริ่นนำ + `.hookq` คำถามเปิด |
| `.fig` | ภาพประกอบพร้อมคำบรรยายใต้ภาพ |
| `.srcnote` | หมายเหตุสำหรับผู้สอน (กรอบเส้นประ) |

## กติกาเนื้อหา

1. **หน่วยเขียนเป็นเศษส่วนซ้อนเสมอ** ใช้ `.frac` ห้ามใช้ `cm³·s⁻¹`
2. **แยกให้ชัดว่าอะไรมาจากหนังสือจริง** — ใช้ `.cite` และเขียน `.srcnote` บอกตรง ๆ ว่าข้อไหนคัดมา ข้อไหนเขียนเอง
3. **ทุกหน้าต้องมีแบบฝึกหัดพร้อมเฉลยแบบพับได้** และควรมี "ตรวจสอบความเข้าใจ" คั่นระหว่างเรียนด้วย
4. **ทุกหน้าควรมีของโต้ตอบอย่างน้อย 1 ชิ้น** ที่ทำให้เห็นมโนทัศน์หลักเคลื่อนไหวได้
5. **สมการในโจทย์ให้นักเรียนดุลเอง** ถ้าจุดประสงค์คือฝึกสัมประสิทธิ์

## วิธีสร้างภาพประกอบ (ผ่าน codex MCP)

โจทย์สไตล์ที่ใช้ได้ผล — ต้องระบุครบทุกข้อนี้เพื่อให้ภาพชุดใหม่เข้ากับของเดิม:

```
Clean flat vector educational-diagram style, like a modern science textbook
figure. NOT photorealistic, NOT 3D, NOT cluttered. Thin confident linework.
Palette: background #F7F8F6, ink/linework #171D1B, cool accent #17457F
(cobalt), second accent #C0301A (vermilion) used sparingly — at most one
small element per image.
NO embedded text, words, numbers or labels baked into the image.
~4:3 ratio, 900-1200px wide.
```

เสร็จแล้ว**ต้องฝัง base64** ก่อน publish (CSP ของ Artifact บล็อกไฟล์ภายนอก):

```js
// _inline.mjs — รันแล้วลบทิ้ง
import { readFileSync, writeFileSync } from "fs";
const file = "0X-xxx.html";
let html = readFileSync(file, "utf8");
for (const name of ["img-aaa", "img-bbb"]) {
  const b64 = readFileSync(`${name}.png`).toString("base64");
  html = html.split(`src="${name}.png"`).join(`src="data:image/png;base64,${b64}"`);
}
writeFileSync(file, html, "utf8");
```

## เช็กก่อน publish ทุกครั้ง

```bash
grep -o '<div' FILE.html | wc -l; grep -o '</div>' FILE.html | wc -l
grep -o '<section' FILE.html | wc -l; grep -o '</section>' FILE.html | wc -l
grep -o '<details' FILE.html | wc -l; grep -o '</details>' FILE.html | wc -l
grep -o '<svg' FILE.html | wc -l; grep -o '</svg>' FILE.html | wc -l
```
ตัวเลขต้องเท่ากันทุกคู่

## ขั้นตอนเผยแพร่

1. ทำหน้าเสร็จ → ฝัง base64 → เช็ก tag balance
2. เปิดดูในเบราว์เซอร์จริงก่อน (อย่าเชื่อว่าโค้ดถูกแล้วจะแสดงผลถูก)
3. แก้ลิงก์ใน `index.html` (เปลี่ยน `class="soon"` เป็น `href=`)
4. `git add` → `commit` → `push`
5. GitHub Pages rebuild เองประมาณ 1 นาที

---

## แหล่งอ้างอิงในโฟลเดอร์ `สสวท book/`

คู่มือครู เคมี ม.5 เล่ม 3 (ฉบับปรับปรุง พ.ค. 66) — บทที่ 8 อัตราการเกิดปฏิกิริยาเคมี

| หน้า | เนื้อหา | ใช้กับมโนทัศน์ |
|---|---|---|
| 54–55 | เปิดบท (แอร์แบ็ก NaN₃) + จุดประสงค์การเรียนรู้ | 7 |
| 56–57 | ความหมายของอัตรา | 1 |
| 58–59 | กิจกรรม 8.1 (Mg + HCl ชุดกระบอกฉีดยา) | 1 |
| 60–63 | นิยามเชิงคณิตศาสตร์ + **ตาราง 8.1** (2NO₂→2NO+O₂ ข้อมูลจริง) | 1 |
| 64–65 | ตาราง 8.2 อัตราเทียบสัมประสิทธิ์ | 1 |
| 68–69 | **กราฟรูป 8.2** + วิธีหาอัตรา ณ ขณะหนึ่งด้วยเส้นสัมผัส | 1 |
| 70–71 | กิจกรรม 8.3 (ข้อมูล Zn + HCl / A→B+C) | 1 |
| 72–73 | **แบบฝึกหัด 8.1** + แนวคิดทฤษฎีการชน | 1, 2 |
| 74–75 | ทิศทางการชน (NO + NO₃) + สารเชิงซ้อนกัมมันต์ | 2 |
| 76–79 | **แผนภาพพลังงาน รูป 8.5–8.7** + แบบฝึกหัด 8.2 | 3 |
| 80–81 | **กิจกรรม 8.4** (CaCO₃ ก้อน/ผง + HCl 0.2/0.5 M) | 4 |
| 82–83 | รูป 8.8, 8.9 (อนุภาค) + **รูป 8.10 การกระจายพลังงานที่ T₁, T₂** | 2, 4 |
| 84–85 | **กิจกรรม 8.5** (H₂O₂ + KI ตัวเร่ง) | 4 |
| 86–87 | รูป 8.11 (ตัวเร่งลด Eₐ) + กิจกรรม 8.6 | 3, 4 |
| 88–89 | **แบบฝึกหัด 8.3** (ตารางการทดลองหาอันดับ) | 5 |
| 90 | สรุปแนวคิดสำคัญประจำบท | 7 |
| 92–93 | แบบฝึกหัดท้ายบท (KClO₃, CH₃COOH + NaHCO₃) | 4, 7 |
| 97–120 | **เฉลยแบบฝึกหัด 8.1 แบบละเอียด** (สไลด์) | 1 |
