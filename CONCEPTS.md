# อัตราการเกิดปฏิกิริยาเคมี — แผนมโนทัศน์ทั้งชุด (6 มโนทัศน์)

เอกสารส่งต่องาน · เคมี ม.5 · บทที่ 8 ตามหนังสือเรียน สสวท.
อัปเดตล่าสุด: 6 กันยายน 2569

repo: https://github.com/Nonsea-teacher/Chemical-reaction-rate
เว็บ: https://nonsea-teacher.github.io/Chemical-reaction-rate/

---

## 🚨 มีงานที่ยังไม่ได้ commit/push ณ ตอนอัปเดตนี้

ทำงานตรงนี้เยอะแล้ว **ยังไม่ได้ `git commit` เลยสักครั้ง** — ถ้าจะย้ายไปทำต่อเครื่องอื่น อย่าลืมว่า `git clone`/`git pull` จะไม่ได้งานพวกนี้มาด้วย **ต้องก๊อปโฟลเดอร์ทั้งหมดข้ามเครื่องเอง** (USB/cloud) จนกว่าจะ push งานนี้ขึ้น remote ก่อน

ไฟล์ที่แก้ไขแต่ยังไม่ commit ณ ตอนนี้ (เช็คด้วย `git status` เพื่อความชัวร์อีกที เผื่อ session หลังมี commit ไปแล้ว):
- `01-rate-definition.html` — เพิ่ม `<meta name="viewport">` ที่หายไปทั้งไฟล์ (บั๊กใหญ่ ทำให้มือถือ/แท็บเล็ตจริงเปิดแล้วซูมย่อทั้งหน้า) + แก้ `.plate` mobile
- `02-collision-theory.html` — แก้ `.plate` mobile เท่านั้น (เนื้อหาไม่เปลี่ยน)
- `03-factors.html` — แก้กราฟ 3.1 (เพิ่มเนินพลังงานเทียบไอออนิก/โคเวเลนต์), แก้ป้ายปุ่มหั่นลูกบาศก์, ปรับ UI กล่อง `.traps`, ลบ `.srcnote` ท้ายบท, แก้ mobile หลายจุด (ปุ่มล้นขอบ, ตัวหนังสือกราฟเล็กไป, กราฟตัวเร่งฯ สลับเป็นแนวตั้งบนจอแคบ), แก้ `.plate` mobile
- `04-rate-law.html` — **ไฟล์ใหม่ทั้งไฟล์** (มโนทัศน์ 4 ฉบับสมบูรณ์)
- `index.html` — เปลี่ยนลิงก์ 04 จาก `soon` เป็น `href`, แก้ `.plate` mobile
- `CONCEPTS.md` — ฉบับนี้เอง

ดูรายละเอียดการแก้ไขแต่ละอย่างในหัวข้อของมโนทัศน์นั้น ๆ ด้านล่าง

---

## สถานะปัจจุบัน

| # | มโนทัศน์ | ไฟล์ | สถานะ |
|---|---|---|---|
| 01 | อัตราการเกิดปฏิกิริยาคืออะไร | `01-rate-definition.html` | ✅ เสร็จ |
| 02 | ทฤษฎีการชน (รวม Eₐ ไปกลับ/ΔE, กราฟหลายยอด, ตัวเร่งปฏิกิริยา — เดิมคือมโนทัศน์ 2+3) | `02-collision-theory.html` | ✅ เสร็จ |
| 03 | ปัจจัยที่มีผลต่ออัตราการเกิดปฏิกิริยา | `03-factors.html` | ✅ เสร็จ |
| 04 | กฎอัตราและอันดับของปฏิกิริยา | `04-rate-law.html` | ✅ เสร็จ |
| 05 | กลไกของปฏิกิริยา | `05-mechanism.html` | ⬜ ยังไม่เริ่ม |
| 06 | การเชื่อมโยงและการประยุกต์ | `06-applications.html` | ⬜ ยังไม่เริ่ม |

> **หมายเหตุการรวมมโนทัศน์:** มโนทัศน์ที่ 3 เดิม (แผนภาพพลังงานของปฏิกิริยา: Eₐ ไปกลับ, กราฟหลายยอด/ขั้นกำหนดอัตรา, ตัวเร่งปฏิกิริยา) ถูกยุบรวมเข้าไปในมโนทัศน์ 2 ทั้งหมดแล้ว (ดูหัวข้อ 2.3–2.5 ในไฟล์ `02-collision-theory.html`) เพื่อความกระชับ ชุดจึงเหลือ 6 มโนทัศน์ และเลขไฟล์ 4–7 เดิมขยับลงมาเป็น 3–6

`index.html` = สารบัญเชื่อมทุกหน้า — **ทำหน้าใหม่เสร็จแล้วอย่าลืมมาแก้ลิงก์ในนี้ด้วย**
(เปลี่ยน `<a class="soon">` เป็น `<a href="0X-....html">` และแก้ตัวเลข "X / 6 พร้อมใช้" ที่หัวหน้า)

---

## บริบทของงาน (สำคัญ ต้องอ่านก่อนทำต่อ)

- **ผู้ใช้หลักคือครู** ที่ยืนสอนหน้าห้องและฉายหน้านี้ขึ้นโปรเจกเตอร์ ไม่ใช่นักเรียนอ่านเอง
  → ตัวหนังสือใหญ่ คอนทราสต์สูง อ่านจากหลังห้องได้ พื้นสว่างเพื่อสู้แสงในห้องที่เปิดไฟ
- **นักเรียนเปิดซ้ำในมือถือทีหลังได้** → ต้องไม่พังบนจอแคบ
- **โหมดสว่างอย่างเดียว** ไม่ทำ dark mode (ผู้ใช้สั่งไว้ชัดเจน)
- **หนึ่งมโนทัศน์ = หนึ่งไฟล์ HTML ที่ยืนได้ด้วยตัวเอง** ไม่มี build step ไม่มี dependency ภายนอก
  (ยกเว้น Google Fonts ซึ่งเป็นโฮสต์เดียวที่ CSP ของ Artifact ยอมให้โหลด)

---

## 🔍 ขอบเขตหลักสูตร (ตรวจกับหนังสือแล้ว — อย่าเดาใหม่)

เปิดหน้า 55 (จุดประสงค์การเรียนรู้ประจำบท 8) ครบทั้ง 8 ข้อ แล้วแมปได้ดังนี้

| จุดประสงค์ | เนื้อหา | ตรงกับมโนทัศน์ |
|---|---|---|
| 1–4 | ความหมาย/คำนวณอัตรา · กราฟปริมาณ–เวลา · กราฟสารที่ไม่ได้วัด | 1 |
| 5 | อธิบายด้วยทฤษฎีการชนและทฤษฎีสถานะแทรนซิชัน | 2 |
| 6–7 | ผลของความเข้มข้น พื้นที่ผิว อุณหภูมิ ตัวเร่ง และการเปรียบเทียบ | 3 |
| 8 | สืบค้นกระบวนการในชีวิตประจำวัน/อุตสาหกรรม | 6 |

**ไม่มีจุดประสงค์ข้อใดพูดถึงกฎอัตรา อันดับปฏิกิริยา หรือกลไกปฏิกิริยาเลย** → มโนทัศน์ 4 และ 5 เป็นเนื้อหาต่อยอดนอกหนังสือ (เพื่อสอบเข้ามหาลัย)

> **ผู้ใช้สั่งไว้:** ทำมโนทัศน์ 4 ต่อได้ แต่ **ห้ามใส่ป้าย/ข้อความบอกว่า "นอกหลักสูตร"** ในหน้าเว็บ ให้นำเสนอเป็นบทเรียนถัดไปตามปกติ

**แก้ความเข้าใจผิดของ CONCEPTS.md ฉบับก่อน:** แบบฝึกหัด 8.3 (หน้า 88–89) **ไม่ใช่โจทย์กฎอัตรา** แต่เป็นแบบฝึกหัดของเรื่อง**ปัจจัย** (= มโนทัศน์ 3) ทั้งชุด 5 ข้อ —
ข้อ 1 เหล็ก+HCl ตัดสินข้อสรุป 5 ข้อ · ข้อ 2 ตาราง A/B → A₂B (ถามเชิงคุณภาพว่าอัตราขึ้นกับ [A] และ [B] อย่างไร ไม่มี k ไม่มีเลขชี้กำลัง) · ข้อ 3 H₂+I₂ ลดปริมาตรภาชนะ · ข้อ 4 Mg เผาไฟ vs วางในอากาศ · ข้อ 5 เนยเทียม+Ni
→ **มโนทัศน์ 3 ยังไม่ได้ใช้แบบฝึกหัด 8.3 เลย** (ตอนนี้ใช้แค่โจทย์ชวนคิดหน้า 84 + กิจกรรม 8.4) ถ้าจะเสริมให้ครบตามหนังสือ ควรเอา 5 ข้อนี้ไปเติมใน `03-factors.html`

---

## ⚠️ เรื่องที่ต้องรู้ก่อนไปทำต่อเครื่องอื่น

1. **โฟลเดอร์ `สสวท book/` ไม่ได้อยู่ใน git** (ใส่ `.gitignore` ไว้ เพราะเป็นสแกนหนังสือมีลิขสิทธิ์ และ repo นี้เป็น public)
   → **ต้องก๊อปโฟลเดอร์นี้ไปเครื่องใหม่ด้วยมือ** (USB / cloud drive) ไม่งั้นจะไม่มีต้นฉบับโจทย์และเนื้อหาให้อ้างอิง
   ข้างในมี: คู่มือครู เคมี ม.5 เล่ม 3 (ฉบับปรับปรุง พ.ค. 66) PDF + ภาพถ่ายหน้าหนังสือ 19 รูป (หน้า 54–120)

2. **ไฟล์ `01-rate-definition.html` ใหญ่ ~2.9 MB** เพราะฝังภาพ 8 ภาพเป็น base64
   → เครื่องมือพรีวิวบางตัวเปิดไม่ไหว ถ้าจะดูให้เปิดผ่าน GitHub Pages หรือเปิดไฟล์ในเบราว์เซอร์ตรง ๆ

3. **ไฟล์ `img-*.png` ต้นฉบับยังเก็บไว้ใน repo** ถึงแม้จะฝัง base64 ในหน้าเว็บแล้ว (มโนทัศน์ 1, 2 เท่านั้น)
   → เก็บไว้เผื่อแก้/ครอปใหม่ ไม่ต้องแกะกลับจาก base64

4. **มโนทัศน์ 3 ไม่มีภาพ raster เลยแม้แต่รูปเดียว** — ผู้ใช้ทดลองให้ codex เจนภาพ 10 รูปแล้วปฏิเสธ
   ("เปลี่ยนเป็นไม่ใช้รูปเลยดีกว่า ใช้ svg เป็นหลัก") ให้ **ตัวอย่างชีวิตจริงเขียนเป็นข้อความ** (กล่อง `.reallife`)
   ส่วนขวดสาธิต/วัตถุใช้ SVG วาดด้วยโค้ดทั้งหมด (ดูรูปแบบ "ขวดรูปกรวยที่มีของเหลว/ฟองลอย/ก้อนของแข็ง" ด้านล่าง)
   → **อย่าเจนภาพ raster ให้มโนทัศน์ 3 อีก** ถ้าจะทำมโนทัศน์ 4–6 ให้ถามผู้ใช้ก่อนว่าอยากได้แนวไหน
     อย่าสมมติว่าอยากได้ codex image เหมือนแผนเดิมใน CONCEPTS.md ฉบับก่อนหน้า

5. **วิธีอ่านภาพถ่ายหนังสือใน `สสวท book/`** — ภาพถ่ายมาแบบ**ตะแคง 90°** และ**เรียงย้อนหลัง** (เลขไฟล์มากขึ้น = หน้าน้อยลง)
   จุดอ้างอิงที่ยืนยันแล้ว: `S__30269464` = หน้า 54–55 · `S__30269459` = 62–63 · `S__30269447` = 84–85 · `S__30269445` = 88–89
   (ประมาณคร่าว ๆ: เลขไฟล์ลดลง 1 ≈ หน้าเพิ่มขึ้น ~2)
   อ่านได้ด้วยการหมุน + ครอป + ขยายก่อน แล้วค่อยเปิดดู (ติดตั้ง Pillow ไว้แล้วในเครื่องนี้):
   ```python
   from PIL import Image
   im = Image.open('สสวท book/S__30269445_0.jpg')
   r = im.rotate(-90, expand=True)          # ทำให้ตัวหนังสือตั้งตรง
   r = r.resize((int(r.width*1.4), int(r.height*1.4)), Image.LANCZOS)
   r.save('<scratchpad>/out.png')           # แล้วค่อย Read ไฟล์นี้
   ```
   ถ้าตัวหนังสือยังเล็กเกินอ่าน ให้ครอปเฉพาะครึ่งหน้าที่ต้องการแล้วขยาย 2 เท่า

6. **พรีวิวไฟล์ผ่าน `file://` ใช้ไม่ได้กับไฟล์ที่ฝัง base64 เยอะ** (มโนทัศน์ 1 โดยเฉพาะ, ~2.9 MB)
   เครื่องมือพรีวิวจะฟ้อง "couldn't open... file may be missing" ทั้งที่ไฟล์ปกติดี
   → รัน local server แทน แต่ `python -m http.server` เพียว ๆ **ไม่ส่ง header `charset=utf-8`**
     ทำให้ตัวอักษรไทยเพี้ยนเป็นเอเลี่ยนในเบราว์เซอร์ (ไฟล์จริงไม่ได้เสีย แค่เดา encoding ผิด)
     ใช้ `.claude/utf8_server.py <port>` แทน (สร้างไว้แล้วในโฟลเดอร์นี้ — ส่ง `Content-Type: text/html; charset=utf-8` ให้ถูกต้อง)
     ไฟล์นี้อยู่ใน `.claude/` ซึ่งไม่ได้ push ขึ้น git เพราะเป็นเครื่องมือเฉพาะเครื่อง ต้องสร้างใหม่เองถ้าย้ายเครื่อง (โค้ดสั้น ๆ ใน git log ของ commit ที่เพิ่มมันก็ได้ หรือขอให้ Claude สร้างใหม่)
   → **local server แบบนี้ยัง cache ฝั่งเบราว์เซอร์เก่งมาก** (ไม่ส่ง cache-control header) เวลาแก้ไฟล์แล้วเปิดซ้ำ URL เดิมในเบราว์เซอร์เดิมมักเห็นของเก่า
     วิธีบังคับโหลดใหม่ที่ใช้ได้ผล: ปิดแท็บเก่าแล้วเปิดแท็บใหม่ (`tabs_close` + `tabs_create` + `navigate`) อย่าพยายาม `location.reload()` เฉย ๆ หรือแปะ `?query` ต่อท้าย URL (query string หลอก server ตัวนี้ให้เดา content-type ผิด กลายเป็นเพี้ยน encoding อีกแบบ) และ**อย่าใช้ `document.write()` แทนการ reload** เพราะมันรีเซ็ต viewport emulation ทำให้ `window.innerWidth` กลับไปเป็นค่าเดสก์ท็อป (~980px) ทั้งที่ยังสั่ง mobile preset ไว้อยู่ ทำให้ทดสอบ mobile ได้ผลลัพธ์หลอก

7. **ทุกไฟล์ต้องมี `<meta name="viewport" content="width=device-width,initial-scale=1">`** ไว้บรรทัดบนสุดๆ ของ `<head>` (ก่อน `<link>` font ก็ได้)
   → เจอบั๊กจริงว่า `01-rate-definition.html` ไม่มี tag นี้เลยทั้งไฟล์ (คงตกหล่นตอนสร้างไฟล์แรกสุด) ทำให้มือถือ/แท็บเล็ตจริงคิดว่าเป็นเดสก์ท็อป (~980px) แล้วซูมย่อทั้งหน้าให้พอดีจอ ตัวหนังสือเล็กจิ๋วทั้งหน้า — เพิ่งแก้ไปแล้ว (ดูหัวข้อ "มีงานที่ยังไม่ได้ commit" ด้านบน)
   → **ถ้าทำไฟล์ 05, 06 ใหม่ ให้ก๊อป `<head>` ทั้งชุดจากไฟล์ที่ทำล่าสุด (04) ไม่ใช่ก๊อปจาก 01/02 แล้วมาเติมเอง** เพื่อไม่ให้พลาดจุดนี้ซ้ำ

8. **บั๊ก mobile ของ `.plate` (แผ่นป้ายหัวหน้า) ที่เจอและแก้แล้วในทุกไฟล์** — กล่อง "มโนทัศน์" และ "แผงวัด" ใน `.plate` ไม่มี `flex-grow` เมื่อจอแคบจนต้องขึ้นบรรทัดใหม่คนเดียว กล่องจะแคบแค่ความกว้างเนื้อหา เหลือพื้นที่ว่างในแถวโผล่สีพื้นหลังของ container (`--rule`) ออกมาดูเหมือนกล่องพัง
   → แก้ด้วยการเพิ่ม `@media (max-width:900px){.plate>div{flex:1 1 100%}}` เข้าไปในทุกไฟล์ (01, 02, 03, 04, index.html) — **ถ้าทำไฟล์ 05, 06 ใหม่ อย่าลืมใส่กฎนี้ด้วย** (อยู่ใน media query `max-width:900px` เดียวกับ `.panel`/`.readout` ที่มีอยู่แล้วในทุกไฟล์)

9. **[impeccable] design hook เคยฟ้อง false positive เรื่อง "bounce/elastic easing"** ที่ keyframe ชื่อ `hkBounce` ใน `02-collision-theory.html` (แอนิเมชันโมเลกุลกราซผ่านกันแบบเฉียงในภาพเกริ่นนำ 2.1) — ชื่อ keyframe มีคำว่า "Bounce" แต่ตัว `animation-timing-function` จริงคือ `linear` และเส้นทางเป็นเส้นตรงคงความเร็วสม่ำเสมอ (เช็คแล้วว่า 3 keyframe point มี delta เท่ากันทุกช่วง) ไม่ใช่ bounce/elastic จริง — **เป็น false positive ของ hook ที่ pattern-match จากชื่อ ไม่ใช่พฤติกรรมจริง ไม่ต้องแก้**

---

## มโนทัศน์ที่ 1 — อัตราการเกิดปฏิกิริยาคืออะไร ✅

**ไฟล์:** `01-rate-definition.html` · **Artifact:** https://claude.ai/code/artifact/1949a00f-edd9-4453-932c-045f34b1f447

### สิ่งที่นักเรียนต้องทำได้
1. เขียนนิยาม `Δ[สาร]/Δt` ระบุหน่วย และอธิบายว่าทำไมสารตั้งต้นต้องมีเครื่องหมายลบ
2. คำนวณอัตราเฉลี่ยจากตารางข้อมูล
3. แยกอัตราเฉลี่ย / อัตรา ณ ขณะหนึ่ง / อัตราเริ่มต้น และอ่านจากกราฟได้
4. แปลงอัตราของสารหนึ่งเป็นอีกสารหนึ่งด้วยเลขสัมประสิทธิ์
5. เลือกวิธีวัดอัตราที่เหมาะกับปฏิกิริยาที่กำหนด

### โครงหน้าที่ทำไปแล้ว
- เกริ่นนำ 3 ภาพ (ดินปืน / สนิม / หินงอกหินย้อย) + คำถามเปิด
- **1.1** นิยามและหน่วย + ภาพชุดเก็บแก๊ส + ตารางหน่วย 4 แบบ
- **1.2** อัตราเฉลี่ยคำนวณด้วยมือ + **ตัวอย่างที่ 1** (ตาราง 8.1 NO₂→NO+O₂ ข้อมูลจริง) + ตรวจสอบความเข้าใจ 2 ข้อ + **ตัวอย่างที่ 2** (เซลล์เชื้อเพลิง H₂+O₂)
- **1.3** เฉลี่ย/ขณะหนึ่ง/เริ่มต้น + **แผงกราฟโต้ตอบ (ลากเส้นตัด→เส้นสัมผัส)** + วิธีลากเส้นสัมผัสด้วยมือ 4 ขั้น + แบบฝึกเลือกเส้นสัมผัสที่ถูก + ตรวจสอบความเข้าใจ 2 ข้อ (Zn+HCl จากหนังสือ)
- **1.4** สัมประสิทธิ์ + กราฟ N₂/H₂/NH₃ ความชัน 1:3:2 + คำถาม 3 ข้อ (สมการยังไม่ดุล ให้นักเรียนดุลเอง)
- **1.5** การ์ดวิธีวัด 5 แบบ พร้อมภาพประกอบทุกใบ
- **1.6** กับดัก 4 ข้อ (❌→✅)
- **1.7** แบบฝึกหัด 8.1 ฉบับเต็มจากคู่มือครู 4 ข้อ พร้อมเฉลยละเอียด

### บทเรียนจากการทำหน้านี้ (เอาไปใช้กับหน้าอื่น)
- **ลำดับสำคัญมาก**: ให้นักเรียน**คำนวณด้วยมือก่อน** (1.2) แล้วค่อยเปิดกราฟโต้ตอบ (1.3) เป็นบทสรุปเชิงภาพ — ตอนแรกวางกราฟไว้บนสุดแล้วผู้ใช้บอกว่าเข้าใจยาก
- **ต้องมีภาพ** อย่าให้ทั้งหน้ามีแต่ตัวหนังสือกับกราฟ SVG
- **หน่วยเขียนเป็นเศษส่วนซ้อน** (`.frac`) ไม่ใช่ `cm³·s⁻¹` — นักเรียนมองยาก

---

## มโนทัศน์ที่ 2 — ทฤษฎีการชน ✅

**ไฟล์:** `02-collision-theory.html` · **Artifact:** https://claude.ai/code/artifact/73c90ec5-48cf-4523-8fe8-8b1f44d94c6b

### สิ่งที่นักเรียนต้องทำได้
1. บอกเงื่อนไข 2 ข้อของการชนที่ทำให้เกิดปฏิกิริยา (พลังงาน ≥ Eₐ + ทิศทางถูกต้อง)
2. อธิบายความหมายของ Eₐ และสารเชิงซ้อนกัมมันต์
3. อธิบายว่าอุณหภูมิเปลี่ยนอัตราได้อย่างไร โดยไม่เปลี่ยน Eₐ
4. ใช้ทฤษฎีการชนอธิบายว่าทำไมความเข้มข้น/พื้นที่ผิวจึงมีผล

### โครงหน้าที่ทำไปแล้ว
- เกริ่นนำ 2 ภาพเทียบ (ชนถูกด้าน→เกิดปฏิกิริยา / ชนเฉียง→สะท้อนแยก)
- **2.1** เงื่อนไข 2 ข้อ + ปฏิกิริยา NO+NO₃→2NO₂ (ตัวอย่างเรื่องทิศทางจากหนังสือ หน้า 73–75)
- **2.2** แผนภาพเนินพลังงาน (Eₐ / สารเชิงซ้อนกัมมันต์ / ΔH) เป็น SVG
- **2.3 (จุดเด่น)** กราฟการกระจายพลังงานจลน์ **ลากอุณหภูมิได้** พื้นที่แรเงาโตจาก 5.3% → 27.8% เมื่อลาก T 30→55
- **2.3** Eₐ ไปข้างหน้า/ย้อนกลับ (Eₐ' = Eₐ − ΔE) + กราฟตัวอย่างตัวเลข + กราฟย่อยคาย/ดูดพลังงาน (เดิมคือเนื้อหามโนทัศน์ 3)
- **2.4** ปฏิกิริยาหลายขั้นตอน — ขั้นกำหนดอัตรา + สารมัธยันตร์ (กราฟสองยอด, เดิมคือเนื้อหามโนทัศน์ 3)
- **2.5** ตัวเร่งปฏิกิริยา — ลด Eₐ แต่ไม่แตะ ΔE (ปุ่มสลับมีตัวเร่ง/ไม่มี, เดิมคือเนื้อหามโนทัศน์ 3)
- **แบบฝึกหัดท้ายบท** — แบบฝึกหัด 8.2 จากหนังสือครบ 4 ข้อ (พร้อมเฉลยกางได้)

> **มโนทัศน์ที่ 3 เดิม (แผนภาพพลังงานของปฏิกิริยา) ถูกยุบรวมเข้าไปในมโนทัศน์ 2 ข้างต้นทั้งหมดแล้ว** — ไม่ต้องทำไฟล์ `03-energy-diagram.html` แยกอีก เนื้อหาเดิม (Eₐ ไปกลับ, ΔH/ΔE, กราฟหลายยอด/ขั้นกำหนดอัตรา/สารมัธยันตร์, ตัวเร่งปฏิกิริยา) อยู่ในหัวข้อ 2.3–2.5 ของไฟล์ `02-collision-theory.html` ครบแล้ว

---

## มโนทัศน์ที่ 3 — ปัจจัยที่มีผลต่ออัตราการเกิดปฏิกิริยา ✅

**ไฟล์:** `03-factors.html` · อ้างอิงหนังสือหน้า 78–87 (แบบฝึกหัด 8.2, กิจกรรม 8.4, 8.5)

### สิ่งที่นักเรียนต้องทำได้
อธิบายผลของแต่ละปัจจัย**ด้วยทฤษฎีการชน** ไม่ใช่ท่องจำ — ดูหัวข้อถัดไปว่าออกแบบให้ทำแบบนี้จริงอย่างไร

### แกนของทั้งหน้า: 5 ปัจจัย แต่มีแค่ 3 กลไก
นี่คือสิ่งที่ทำให้หน้านี้ต่างจากแผนเดิม (เดิมจะเขียนเป็นลิสต์ 5 หัวข้อเรียงกัน ซึ่งลองแล้วผู้ใช้บอกว่า "ยังไม่เวิร์ค" เพราะนักเรียนจำได้แค่ว่า "มี 5 ปัจจัย" แต่ไม่เข้าใจว่าทำไมต้องเป็น 5 อันนี้) จึงออกแบบใหม่ทั้งหมดให้ยึดกลไก 3 ทางจากทฤษฎีการชน (มโนทัศน์ 2) เป็นแกนกลาง:

| ปัจจัย | กลไก |
|---|---|
| ธรรมชาติของสาร | กำหนด Eₐ ตั้งต้น (**เปลี่ยนไม่ได้**) |
| ความเข้มข้น | ① ชนบ่อยขึ้น |
| พื้นที่ผิว | ① ชนบ่อยขึ้น |
| อุณหภูมิ | ② อนุภาคที่แรงถึงเกณฑ์มีมากขึ้น |
| ตัวเร่งปฏิกิริยา | ③ ลดเกณฑ์ Eₐ (ปัจจัยเดียวที่แตะ Eₐ จริง) |

ทุกหัวข้อ (3.2–3.5) มีป้าย `.waytag` กำกับที่หัวข้อว่าอยู่ทาง ①/②/③ ไหน และท้ายหน้ามีตารางกดเฉลย (`.synth`) ให้นักเรียนทายกลไกก่อนเปิดดูคำตอบ

### ลำดับหน้าที่ทำจริง
1. เกริ่นนำ — ขวดคู่ผง/ก้อน CaCO₃ (48 วิ vs 300 วิ) พร้อมสมการเต็มใต้ภาพ + คำถามเปิดแบบคำถาม ("...ทำไมใช้เวลาไม่เท่ากัน ?")
2. **3.1 ธรรมชาติของสาร** (เปลี่ยนไม่ได้) — ตัวอย่างจริง: โซเดียมใส่น้ำลุกใน 2-3 วิ vs ตะปูเหล็กแช่น้ำเป็นสัปดาห์กว่าจะขึ้นสนิม (เปลี่ยนจากตัวอย่าง AgNO₃/CH₄ เดิมเพราะพันธะไอออนิก/โคเวเลนต์เป็นนามธรรมเกินไป โลหะสองชนิดในน้ำเดียวกันเห็นภาพชัดกว่า) แล้วค่อยลงลึกด้วยการ์ดพันธะไอออนิก/โคเวเลนต์เดิม
3. **กรอบ 3 ทาง** — อธิบายว่าทำไมมีแค่ 3 ทาง (ย้อนทฤษฎีการชน: ต้องชนกัน + พลังงานถึงเกณฑ์ + หันด้านถูก)
4. **ห้องทดลองการชน (interactive)** — อนุภาคชนกันจริงใน SVG, สีแดง = พลังงานถึงเกณฑ์, นับ "ชนทั้งหมด" กับ "ชนสำเร็จ" (แดงชนแดง) แยกกัน, สไลเดอร์จำนวนอนุภาค + อุณหภูมิ + ปุ่มใส่ตัวเร่ง — ตัวเดียวโชว์ครบทั้ง 3 กลไก
5. **3.2 ความเข้มข้น** `ทาง ①` — น้ำยาล้างห้องน้ำเข้มข้น / ห้องคนแน่น → กราฟแท่ง 48/36 วิ (กิจกรรม 8.4)
6. **3.3 พื้นที่ผิว** `ทาง ①` — หมูชาบูหั่นบาง / น้ำตาลทราย → **ลูกบาศก์หั่นได้ (interactive)** ปุ่มหั่น 0/1/2 ครั้ง เห็นพื้นที่ผิว 96→192→384 ขณะปริมาตรคงที่ 64 + กราฟแท่ง 48/>300 วิ + กราฟ V–t สลับโหมดได้ (ของเดิม)
7. **3.4 อุณหภูมิ** `ทาง ②` — ตู้เย็น/เตาไฟ → กราฟแท่ง 48/25 วิ + **กราฟการกระจายพลังงานลากอุณหภูมิได้ 25–100°C (interactive)** พร้อมเส้นผีของ 25°C ไว้เทียบ
8. **3.5 ตัวเร่งปฏิกิริยา** `ทาง ③` — สับปะรดหมักเนื้อ/น้ำยาล้างแผล → สมการ H₂O₂+KI + ขวดคู่ SVG (ฟองพุ่ง/ไม่ฟู่) + **ปุ่มใส่ตัวเร่ง (interactive)** สลับกราฟพลังงาน+กราฟการกระจายพร้อมกัน เห็นว่า "ขยับเส้น Eₐ" ต่างจาก "ขยับอุณหภูมิ" ยังไง
9. สรุปตาราง 5→3 กดเฉลยทีละแถว
10. แบบฝึกหัดท้ายบท — โจทย์ชวนคิดจากหนังสือ + โจทย์อิงกิจกรรม 8.4

### บทเรียนสำคัญจากการทำหน้านี้ (เอาไปใช้กับมโนทัศน์ 4–6)

**เนื้อหา/pedagogy:**
- **"5 ปัจจัยเรียงกัน" ไม่พอ** ต้องหาแกนกลางที่ร้อยทุกปัจจัยเข้าด้วยกัน (ที่นี่คือ "3 กลไก") ไม่งั้นนักเรียนจำเป็นลิสต์แยกส่วน
- **ตัวอย่างชีวิตจริงต้องตัดตัวแปรกวนออกให้เหลือน้อยที่สุด** — ตัวอย่างเดิม (ตะกอนขาวทันที vs ต้องจุดไฟ) ปนเรื่อง spontaneity เข้ามาโดยไม่ตั้งใจ ตัวอย่างที่ดีกว่า (โซเดียม vs เหล็กในน้ำเดียวกัน) ตัดทุกตัวแปรออกเหลือแค่ "ชนิดของสารต่างกัน"
- **ระวังคำที่ทำให้แนวคิดผิดเพี้ยน** ผู้ใช้จับได้ 2 จุด: "ชิ้นเดียวกัน" (ใช้กับสารต่างชนิดกันไม่ได้ ชวนสับสนว่าเป็นสารเดียวกัน) และ "พล่าน" (เกินจริงเทียบกับปฏิกิริยาจริง) — **อ่านทวนคำเดี่ยว ๆ ในประโยคเทียบกับคอนเซปต์ที่ตั้งใจสื่อ ไม่ใช่แค่อ่านรวมทั้งประโยคว่าลื่นไหม**
- **ภาพต้องสื่อปริมาณที่เท่ากันจริง ไม่ใช่แค่ "ดูคล้ายกัน"** — ตอนแรกวาดผง (~54 จุด) vs ก้อน (4 ชิ้น) แล้วผู้ใช้ทักว่ามวลดูไม่เท่ากันเลย ทั้งที่โจทย์บอกว่าเท่ากัน (0.3 g ทั้งคู่) แก้ 2 ทาง: (1) ใส่ตัวเลข "0.3 g" ในแคปชันให้ชัด (2) ลดจำนวนก้อนแข็งฝั่ง "ก้อน" ให้เหลือก้อนเดียว (ของจริงคือ "ก้อน" ไม่ใช่ "หลายก้อนเล็ก" อยู่แล้ว)

**เทคนิค SVG (ใช้ซ้ำได้กับหน้าถัดไป):**
- **ขวดรูปกรวยที่มีของเหลว/ฟอง/ของแข็งลอย** — วาดด้วยฟังก์ชัน `hw(y)` (half-width ของขวดที่ความสูง y ใดๆ) แล้วคำนวณตำแหน่งวัตถุทุกชิ้น (ฟองอากาศ, ผง, ก้อน) จาก `hw(y)` ของตำแหน่งนั้นเสมอ **ห้ามใช้พิกัดตายตัวที่เดามือ** — บั๊กที่เจอ 2 รอบ (ฟองล้นปากขวด, ก้อนหินทะลุพื้นขวด) ทั้งคู่เกิดจากพิกัดเดามือที่ไม่ผ่าน `hw(y)`
- **วัตถุที่มี animation ต้องเช็ค bounding-box ตลอดเส้นทางที่เคลื่อนที่ ไม่ใช่แค่จุดเกิด** — ฟองลอยขึ้น 52px ด้วย CSS keyframe ต้องคำนวณขอบเขตจากจุดสูงสุดที่จะลอยไปถึง (`hw(y - TRAVEL)`) ไม่ใช่จุดเกิด เพราะขวดแคบลงเมื่อขึ้นไปทางคอ
- **ตรวจ overflow ด้วยสคริปต์วัดพิกัดจริงเทียบกับผนัง ไม่ใช่แค่ดูภาพ** — เขียน JS เช็ค `|cx-flaskCenter|+r <= hw(cy)` แบบ automated แม่นยำกว่าการซูมดูด้วยตา (screenshot zoom ในเบราว์เซอร์นี้ crop ไม่ได้ด้วย)
- **กราฟ Maxwell–Boltzmann ต้องพล็อตบนแกน "อัตราเร็ว" (v) ไม่ใช่แกน "พลังงาน" (E)** — พล็อตบนแกน E จะมียอดกราฟอยู่ที่ E=kT/2 เสมอ ซึ่งถูกบีบติดแกน Y ทุกครั้งที่ Eₐ/kT สูงพอจะให้ตัวเลขสมจริง เปลี่ยนไปพล็อตบนแกน v (สูตร f(v)=v²·exp(-v²/2σ²), ยอดอยู่ที่ v=√2σ) ได้รูปทรงระฆังที่ถูกต้องตามตำรา แล้วใช้ threshold บนแกน v แทน (พลังงานจลน์ ∝ v² อยู่แล้ว เกณฑ์ v ก็คือเกณฑ์ E)
- **`.win .lbl` ต้องมีพื้นหลังกำกับเสมอ** ถ้าไม่ใช้คลาส `is-secant`/`is-tangent` (ซึ่งมีพื้นหลังในตัว) ตัวหนังสือจะเป็นสีขาวบนพื้นขาว มองไม่เห็นเลย — เช็คแล้วว่ามโนทัศน์ 1, 2 ไม่โดนบั๊กนี้ (ไม่มี `.win` เปล่าอยู่)

**อื่น ๆ:**
- **การทำสิมูเลชันฟิสิกส์ (ห้องทดลองการชน) ต้อง "สุ่มพลังงานใหม่ทุกครั้งที่ชน" ไม่ใช่แค่สลับ velocity** — ถ้าสลับกันเฉยๆ จำนวนอนุภาค "แดง" จะถูกล็อกจากการสุ่มครั้งแรกและไม่ขยับตามอุณหภูมิที่ปรับ (ดูเหมือนทำงานแต่ตัวเลขไม่จริง) ต้องสุ่มใหม่ (thermalize) ทุกครั้งที่ชนกันจึงจะตอบสนองต่อสไลเดอร์จริง และควรเฉลี่ยค่าที่แสดงย้อนหลัง ~2.5 วิ ไม่งั้นตัวเลขกระตุกเกินอ่านง่าย
- **เมื่อผู้ใช้บอกให้ลบไฟล์ที่ไม่ใช้แล้ว ให้ grep หาก่อนว่ามีไฟล์ชื่อคล้ายกันที่ยัง track อยู่ใน git หรือไม่** — โฟลเดอร์นี้มี `img-*.png` สองชุดปนกัน (ของมโนทัศน์ 1-2 ที่ยัง track อยู่ กับของมโนทัศน์ 3 ที่ codex เจนแล้วไม่ได้ใช้) ลบผิดไฟล์จะเสียงานเก่า

### แก้ไขเพิ่มเติมหลังทำเสร็จ (รอบตรวจทานที่ 2)

หลังจากทำเสร็จและ commit ไปแล้ว ผู้ใช้ขอให้ปรับต่ออีกหลายจุด (ทำใน session ถัดมา ก่อน push):

1. **เพิ่มกราฟเนินพลังงานใน 3.1** — ผู้ใช้ขอ "ถ้ามีกราฟให้ดูสักเล็กน้อยน่าจะเห็นภาพมากกว่าเดิม" หลังเห็นแค่การ์ดข้อความไอออนิก/โคเวเลนต์ → เพิ่ม mini energy-hill chart 2 เส้นซ้อนกัน (ไอออนิก Eₐ ต่ำ สีโคบอลต์ / โคเวเลนต์ Eₐ สูง สีชาด) เริ่มจากจุดเดียวกัน ใช้เทคนิคเดียวกับ `buildMiniHill` ใน 02 (สร้าง path ด้วย smoothstep แล้วปักลูกศร Eₐ จากเส้นประระดับสารตั้งต้นถึงเส้นประระดับยอดเขา)
2. **แก้ป้ายปุ่ม "หั่น 1 ครั้ง / หั่น 2 ครั้ง" ในลูกบาศก์หั่นได้ (3.3)** — ผู้ใช้ทักว่า "มันดูหั่นเกินกว่านั้นนะ" เพราะภาพจริงแสดงลูกบาศก์แบ่งเป็น 8/64 ก้อน (ต้องหั่น 3 แนวพร้อมกันจึงจะได้ 8 ก้อน ไม่ใช่หั่น "1 ครั้ง" ตามตัวอักษร) → เปลี่ยนป้ายเป็นเรียกตามจำนวนก้อนที่ได้ตรง ๆ: **"หั่นเป็น 8 ก้อน" / "หั่นเป็น 64 ก้อน"** ตัดปัญหาการนับ "ครั้ง" ที่กำกวมออกไปเลย (แก้ทั้งปุ่ม, แถวตาราง, และ label ใน JS)
3. **ปรับ UI กล่อง `.traps` (✗ เข้าใจผิด / ✓ ที่ถูกต้อง)** — ของเดิมป้ายกับข้อความแยกเป็นคอลัมน์บาง ๆ ไม่มีสีช่วยจับคู่ ผู้ใช้บอกว่า "ดูยาก" → เปลี่ยนเป็นบล็อกสีทึบทั้งคู่ (ป้าย+ข้อความของแต่ละฝั่งอยู่ในบล็อกสีเดียวกัน — แดงอ่อนสำหรับ "เข้าใจผิด", น้ำเงินอ่อนสำหรับ "ที่ถูกต้อง") ด้วย CSS grid-template-areas ที่สลับ layout ระหว่างมือถือ (1 คอลัมน์) กับจอกว้าง (label คอลัมน์แคบ + เนื้อหาคอลัมน์กว้าง) **การเปลี่ยนนี้กระทบแค่ CSS ของ `.traps` ใน 03 เท่านั้น** ยังไม่ได้ทำให้เข้ากับ 01/02 ที่ใช้ `.traps` แบบเดิม (คอลัมน์บาง ไม่มีสีพื้นหลัง) — ถ้าผู้ใช้อยากให้ธีมนี้เหมือนกันทั้งชุด ต้องไปแก้ 01/02 เพิ่ม
4. **ลบ `.srcnote` ท้ายบทออก** — ผู้ใช้ขอ "เอาอันนี้ออกเลย" ชี้ที่บรรทัดอ้างอิงกิจกรรม 8.4/8.5 ท้ายแบบฝึกหัด (ไม่ได้ให้เหตุผล ทำตามที่สั่งเฉย ๆ)
5. **บั๊ก mobile ที่เจอเพิ่ม (ดูหัวข้อ "เรื่องที่ต้องรู้ก่อนไปทำต่อเครื่องอื่น" ข้อ 8 ด้วย):**
   - ปุ่มหั่นลูกบาศก์ + ปุ่ม "เทียบความเข้มข้น/เทียบพื้นที่ผิว" ล้นขอบจอมือถือ เพราะกลุ่มปุ่มตั้ง `flex:0 0 auto` ไม่ยอม wrap → เพิ่มคลาส `.grp.btns{flex-wrap:wrap}` ให้ปุ่มขึ้นบรรทัดใหม่แทนล้น
   - ตัวหนังสือในกราฟ 3.1 (เนินพลังงาน) ใช้ font-size 12–14 เล็กกว่ามาตรฐานไฟล์นี้ (16–17) พอย่อมือถือเลยอ่านไม่ออก → ปรับขึ้นให้ตรงมาตรฐาน
   - **กราฟตัวเร่งปฏิกิริยา (3.5)** เป็น SVG เดียว viewBox กว้าง 900 วางกราฟพลังงาน+กราฟการกระจายไว้ข้างกัน พอบีบเหลือ ~330px แต่ละรูปเหลือ ~150px อ่านไม่ออกเลย → ทำให้ **สลับ layout อัตโนมัติเป็นเรียงบนล่างเมื่อจอแคบกว่า 760px** (ฟังก์ชัน `layout()` เปลี่ยน viewBox และพิกัด EX0/EX1/DX0/DX1 ตาม `window.matchMedia`, ผูกกับ `resize` event ที่ debounce ไว้ 150ms) โค้ดวาดกราฟใช้ตัวแปรพิกัดเดิมทั้งหมด ไม่ต้องเขียนใหม่

---

## มโนทัศน์ที่ 4 — กฎอัตราและอันดับของปฏิกิริยา ✅

**ไฟล์:** `04-rate-law.html` · ข้อมูลจริงจากแบบฝึกหัด 8.3 ข้อ 2 (หน้า 88)

> **หมายเหตุ:** นี่คือหัวข้อที่มีน้ำหนักมากที่สุดในข้อสอบเข้ามหาวิทยาลัย ควรลงแรงมากที่สุด

### แกนของทั้งหน้า: "ตัวเลขสองชุดที่หน้าตาเหมือนกัน แต่มาจากคนละโลก"
- **เลขสัมประสิทธิ์** ← ดุลสมการบนกระดาษ → ใช้แปลงอัตราระหว่างสาร (มโนทัศน์ 1) **ใช้ได้จริง**
- **อันดับ m, n** ← วัดจากการทดลองเท่านั้น → ใช้เขียนกฎอัตรา หา k ทำนายอัตรา

**สะพานเชื่อมที่ทำให้หน้านี้ไม่ใช่ของแปะเพิ่ม:** ใช้ตาราง A/B จากแบบฝึกหัด 8.3 ข้อ 2 เป็นตัวเปิด — เพิ่ม [B] 3 เท่าแล้วผลิตภัณฑ์เท่าเดิมเป๊ะ
และเนื่องจากสมการดุลคือ **2A + B → A₂B** ตารางเดียวกันนี้จึง**หักล้างความเชื่อ "อันดับ = สัมประสิทธิ์" ด้วยตัวมันเอง**
(ทำนายจากสัมประสิทธิ์ = k[A]²[B] · วัดจริง = k[A]) — เป็นหลักฐานจากหนังสือของครูเอง ไม่ต้องไปหาตัวอย่างที่อื่น

### โครงหน้าที่ทำจริง
1. เกริ่นนำ — กราฟแท่ง**แนวนอน**รวมกับตารางข้อมูล 4 การทดลองจากหนังสือในภาพเดียว (แท่ง 1–3 จบที่ตำแหน่งเดียวกันเป๊ะทั้งที่ [B] เพิ่ม 3 เท่า มีเส้นประช่วยตาว่า "จบตรงกัน", แท่ง 4 สีชาดสั้นลงครึ่งหนึ่ง) + คำถามเปิด — **ผ่านการวนออกแบบ 3 รอบ** ก่อนจะลงตัว (ดูหัวข้อ "บทเรียนจากการทำหน้านี้" ด้านล่าง เรื่องทำไมถึงเปลี่ยนจากกราฟ+ตารางแยกกัน → กราฟ+ตารางคอลัมน์ตรงกันแนวตั้ง → กราฟ+ตารางรวมกันแนวนอน)
2. **4.1** กฎอัตรา `rate = k[A]ᵐ[B]ⁿ` — อธิบายทีละสัญลักษณ์
3. **4.2** m, n มาจากไหน (แกนกลาง) — การ์ดสองโลก + หลักฐานจากตารางหนังสือ + กับดัก 2 ข้อ
4. **4.3** วิธีอัตราเริ่มต้น — ขั้นตอน 4 ขั้น + **[ของโต้ตอบชิ้นเอก] ตารางเลือกคู่เทียบ** + ตรวจสอบความเข้าใจ 2 ข้อ
5. **4.4** ความหมายของอันดับ 0/1/2 — **[กราฟ rate–[A] สลับอันดับได้]** + ตัวอย่างชีวิตจริง (แอลกอฮอล์ในเลือด = อันดับศูนย์)
6. **4.5** ค่า k และหน่วย — **[เครื่องคิดหน่วยของ k]** + ตัวอย่างหา k ครบขั้นตอน + ตาราง "ปัจจัยไหนเปลี่ยน k"
7. แบบฝึกหัด — แบบฝึกหัด 8.3 ข้อ 2 จากหนังสือ (2.1, 2.2) + โจทย์ครบวงจรแนวข้อสอบ 3 ข้อ

### ของโต้ตอบ 3 ตัว
- **ตารางเลือกคู่เทียบ (ชิ้นเอก)** — กดเลือก 2 แถว ระบบตรวจให้ว่าคู่นั้น**ใช้หาอันดับได้หรือไม่** ถ้าเปลี่ยนสองอย่างพร้อมกันขึ้นกล่องแดงอธิบายว่าทำไมใช้ไม่ได้ ถ้าใช้ได้จะกางขั้นตอนคำนวณให้ครบจนได้อันดับ · สลับได้ 2 ชุดข้อมูล (ชุดหนังสือ → ได้อันดับ 0 กับ 1 · ชุดแนวข้อสอบ → ได้อันดับ 2) · ตรรกะเป็น generic คำนวณจาก `log(rateRatio)/log(concRatio)` ไม่ได้ hard-code คำตอบ
- **กราฟ rate–[A] สลับอันดับ 0/1/2** — เห็นรูปทรงเปลี่ยน (เส้นแบน/เส้นตรง/พาราโบลา) พร้อมจุดตรวจสองจุดที่ห่างกัน 2 เท่า และวงเล็บบอก ×1 / ×2 / ×4
- **เครื่องคิดหน่วยของ k** — กดเลือกอันดับรวม 0–3 แล้วโชว์หน่วยพร้อมที่มา (ใช้ `.frac` ตามกติกา)

### บทเรียนจากการทำหน้านี้
- **ตรวจขอบเขตหลักสูตรกับหนังสือก่อนวางแผนเสมอ** — แผนเดิมใน CONCEPTS.md เข้าใจผิดว่าแบบฝึกหัด 8.3 เป็นโจทย์กฎอัตรา พอเปิดหนังสือจริงถึงรู้ว่าเป็นโจทย์เรื่องปัจจัย และทั้งหัวข้ออยู่นอกจุดประสงค์ ม.5 (ดูหัวข้อ "ขอบเขตหลักสูตร" ด้านบน)
- **ข้อมูลจากหนังสือที่ "หักล้างความเข้าใจผิดได้ด้วยตัวเอง" มีค่ามาก** — ดีกว่ายกตัวอย่างลอย ๆ จากที่อื่นเยอะ เพราะนักเรียนเถียงไม่ได้ว่าเป็นกรณีพิเศษ
- **ป้ายแกนกราฟ SVG ชนกันได้ง่ายเมื่อมีคำอธิบายหลายชั้น** — เจอ 2 รอบในหน้านี้ (ป้ายแกน Y ชนวงเล็บปีกกาด้านบน · ป้ายแกน X ชนป้ายจุด c/2c) วิธีแก้คือหมุนป้ายแกน Y ไปวางตั้งตามขอบซ้าย และย้ายป้ายแกน X ลงไปใต้ป้ายจุด **ต้องเปิดดูจริงทุกครั้ง อย่าเชื่อว่าโค้ดถูกแล้วจะไม่ทับกัน**
- **เผื่อขอบขวาของ viewBox ไว้ให้ป้ายที่วางนอกพื้นที่กราฟ** — วงเล็บ ×N วางที่ `sx(1.0)+22` ซึ่งเลยขอบ viewBox เดิมไป ทำให้ตัวหนังสือถูกตัดหาย แก้โดยขยาย viewBox แล้วหด X1 เข้ามา
- **กราฟ+ตารางข้อมูลควรรวมเป็นภาพเดียวโดยให้ "แถว" หรือ "คอลัมน์" ตรงกัน ไม่ใช่วางแยกกันคนละกล่อง** — จุดเริ่มต้นของ hook chart คือแท่งกราฟ (บนตาราง) กับป้าย [A]/[B] (ใต้แท่ง) แยกกันคนละส่วน ผู้ใช้บอกว่า "อยากให้ดูง่ายกว่านี้ มีตารางให้เห็นก่อนจะช่วยไหม" → **รอบแรก**ลองทำเป็นตารางแนวตั้งคอลัมน์ตรงกับแท่ง (ตัวเลขอยู่ใต้แท่งเป๊ะ) ผู้ใช้บอกว่า "ยังรู้สึกว่ามองยากอยู่" → **รอบสอง**เปลี่ยนเป็นแนวนอน (1 การทดลอง = 1 แถว, แท่งวิ่งออกทางขวาโดยตรงในแถวเดียวกับตัวเลข [A]/[B]) จึงอ่านจบในบรรทัดเดียวได้จริง ไม่ต้องกวาดตาขึ้นลง — **บทเรียน: ถ้าอยากให้กราฟกับตารางเป็นเรื่องเดียวกัน ให้ลองแนวนอนก่อน (แท่งอยู่ปลายแถว) ไม่ใช่แนวตั้ง (แท่งอยู่บนหัวคอลัมน์)** เพราะสายตาคนอ่านตารางเป็นแถวอยู่แล้ว
- **ผลพลอยได้จากแนวนอน: ใช้ตำแหน่งปลายแท่งเป็นหลักฐานภาพได้เลย** — เมื่อแท่ง 1–3 ยาวเท่ากัน (เพราะค่าเท่ากัน) ปลายแท่งทั้งสามจะตกที่ตำแหน่ง x เดียวกันพอดี จึงลากเส้นประแนวตั้งผ่านจุดนั้นได้ กลายเป็นหลักฐานที่ตาจับได้ทันทีว่า "จบที่เดียวกัน" โดยไม่ต้องอ่านตัวเลข — ไม่มีผลแบบนี้ถ้าเป็นกราฟแนวตั้ง

### สิ่งที่นักเรียนต้องทำได้
1. เขียน `rate = k[A]^m[B]^n` และรู้ว่า **m, n หาจากการทดลองเท่านั้น ไม่ใช่จากเลขสัมประสิทธิ์** ← หัวใจ
2. หาอันดับด้วย**วิธีอัตราเริ่มต้น** (คุมตัวแปรอื่นคงที่ เทียบทีละคู่)
3. หาอันดับรวม m + n และเข้าใจความหมายของอันดับศูนย์
4. **หาค่า k พร้อมหน่วย** — หน่วยเปลี่ยนตามอันดับรวม: `(mol·dm⁻³)^(1−รวม)·s⁻¹` (เด็กตอบหน่วยผิดบ่อย)
5. รู้ว่า k ขึ้นกับอุณหภูมิและตัวเร่งเท่านั้น ไม่ขึ้นกับความเข้มข้น

### แนวคิดการออกแบบหน้า
- **จุดเด่นที่ควรทำ**: ตารางการทดลองแบบ**โต้ตอบ** — คลิกเลือกสองแถวที่จะเทียบ แล้วระบบไฮไลต์ว่าตัวแปรไหนคงที่/ตัวไหนเปลี่ยน พร้อมโชว์อัตราส่วนที่คำนวณได้ → ฝึกทักษะ "เลือกคู่ที่ถูก" ซึ่งเป็นจุดที่นักเรียนพลาด
- ข้อมูลจริงจากแบบฝึกหัด 8.3 ข้อ 2 (หน้า 88): สาร A, B → A₂B มี 4 การทดลอง
  | การทดลอง | A เริ่มต้น (M) | B เริ่มต้น (M) | A₂B ที่ 10 นาที (M) |
  |---|---|---|---|
  | 1 | 1.00 | 0.50 | 0.40 |
  | 2 | 1.00 | 1.00 | 0.40 |
  | 3 | 1.00 | 1.50 | 0.40 |
  | 4 | 0.50 | 0.50 | 0.20 |
- ควรมีเครื่องคำนวณหน่วยของ k ที่อัปเดตตามอันดับรวมที่เลือก
- รูปแบบข้อสอบมาตรฐาน: ตาราง 3–4 แถว → หาอันดับ → เขียนกฎอัตรา → หา k → ทำนายอัตราการทดลองที่ 5 (ครบวงจรในข้อเดียว)

---

## มโนทัศน์ที่ 5 — กลไกของปฏิกิริยา ⬜

**ไฟล์ที่จะทำ:** `05-mechanism.html`

### สิ่งที่นักเรียนต้องทำได้
1. เข้าใจว่าปฏิกิริยาส่วนใหญ่เกิดหลายขั้น ผลรวมทุกขั้น = สมการรวม
2. ระบุ**ขั้นกำหนดอัตรา** (ขั้นที่ช้าที่สุด / Eₐ สูงสุด) และรู้ว่ากฎอัตราสอดคล้องกับสารตั้งต้นของขั้นนี้เท่านั้น
3. ใช้กฎอัตราจากการทดลองมา**ตรวจสอบว่ากลไกที่เสนอสมเหตุสมผลหรือไม่**
4. แยก **โมเลกุลาริตี** (จากกลไก) ออกจาก **อันดับ** (จากการทดลอง)
5. แยก **สารมัธยันตร์** (เกิดขั้นแรก ใช้หมดขั้นหลัง) จาก **ตัวเร่ง** (เข้าขั้นแรก ออกขั้นหลัง)

### แนวคิดการออกแบบหน้า
- **จุดเด่นที่ควรทำ**: แผนภาพพลังงานหลายยอดที่**คลิกแต่ละขั้นได้** แล้วโชว์สมการขั้นย่อยนั้น + ไฮไลต์ว่าขั้นไหนคือคอขวด
- อุปมา "คอขวด" / "ทางด่วนช่องเดียว" — เร่งขั้นที่เร็วอยู่แล้วไม่ช่วยอะไร
- ตัวอย่าง NO + NO₃ → 2NO₂ (ต่อยอดจากมโนทัศน์ 2 ที่ใช้ปฏิกิริยานี้แล้ว) หรือ N₂O₅
- แบบฝึกหัด: ให้กลไก 2 แบบ + กฎอัตราจากการทดลอง แล้วถามว่ากลไกไหนสอดคล้อง

> นี่คือมโนทัศน์ที่ยากที่สุดและแยกเด็กคะแนนสูงออกจากเด็กท่องจำ

---

## มโนทัศน์ที่ 6 — การเชื่อมโยงและการประยุกต์ ⬜

**ไฟล์ที่จะทำ:** `06-applications.html` · อ้างอิงหนังสือหน้า 90 (สรุปแนวคิดสำคัญ) และหน้า 54 (แอร์แบ็ก NaN₃)

### สิ่งที่นักเรียนต้องทำได้
1. **อัตรา ≠ สมดุล**: ตัวเร่งเปลี่ยนอัตราแต่ไม่เปลี่ยน K / อุณหภูมิเปลี่ยนทั้งอัตราและ K / ปฏิกิริยาที่เอื้อทางพลังงานอาจเกิดช้ามากถ้า Eₐ สูง (เพชร → แกรไฟต์)
2. เชื่อมกับปริมาณสัมพันธ์: สารกำหนดปริมาณคุม "ปริมาณผลิตภัณฑ์สุดท้าย" ส่วนปัจจัยด้านอัตราคุม "ความชันของกราฟ"
3. อธิบายการประยุกต์ในชีวิตจริงด้วยหลักการจากบทนี้

### แนวคิดการออกแบบหน้า
- **จุดเด่นที่ควรทำ**: ผังสรุปทั้ง 6 มโนทัศน์เชื่อมโยงกัน (concept map) เป็น SVG — ใช้เป็นหน้าทบทวนก่อนสอบได้
- กรณีศึกษา: แอร์แบ็ก (NaN₃ ต้องเร็วมาก — หนังสือใช้เปิดบท), การถนอมอาหาร/ตู้เย็น, เครื่องฟอกไอเสียรถยนต์, กระบวนการฮาเบอร์, เอนไซม์ในร่างกาย, สารกันหืน
- ข้อสอบแนวสถานการณ์ (context-based) ใช้ส่วนนี้เยอะขึ้นเรื่อย ๆ
- ควรมีตารางสรุป "ปัจจัยไหนเปลี่ยน k / ปัจจัยไหนเปลี่ยน K / ปัจจัยไหนเปลี่ยนทั้งคู่"

---

# คู่มือทางเทคนิค (ทำหน้าใหม่ให้เหมือนของเดิม)

## วิธีเริ่มหน้าใหม่ที่เร็วที่สุด
ก๊อป `02-collision-theory.html` เป็นไฟล์ใหม่ แล้วแก้เนื้อหา — ไฟล์นี้มี CSS ครบทุกคอมโพเนนต์และเล็กกว่าไฟล์ 01 มาก (ไม่มีภาพ base64 เยอะ)

## Design tokens (ห้ามเปลี่ยน — ใช้ร่วมกันทั้งชุด)

```css
--ground:#D9DEDA;   /* พื้นหลังนอกแผง */
--panel:#F7F8F6;    /* พื้นแผงอีนาเมล */
--panel-2:#ECEFEB;  /* พื้นรอง */
--ink:#171D1B;      /* หมึกหลัก */
--ink-2:#5A6461;    /* หมึกรอง */
--ink-3:#8B948F;    /* ป้ายกำกับจาง */
--rule:#BAC2BD;     /* เส้นขอบ */
--rule-2:#D5DBD6;   /* เส้นกริด */
--index:#C0301A;    /* สีชาด = เน้น/เส้นสัมผัส/ผิด */
--index-soft:#F2D9D3;
--cobalt:#17457F;   /* น้ำเงิน = ข้อมูล/เส้นตัด/ถูก */
--cobalt-soft:#D6E0EE;
--sans:"Anuphan";      /* เนื้อความ */
--tech:"Bai Jamjuree"; /* หัวเรื่อง ตัวเลข ป้ายกำกับ */
```

**ความหมายของสี (ใช้ให้สม่ำเสมอ):**
- **น้ำเงินโคบอลต์** = ค่าที่วัดได้ / เส้นตัด / คำตอบถูก / กล่องอ้างอิงหนังสือ
- **สีชาด** = ค่าที่เน้น / เส้นสัมผัส / คำตอบผิด / เลขหัวข้อ

## คลาส CSS ที่มีให้ใช้แล้ว

| คลาส | ใช้ทำอะไร |
|---|---|
| `.plate` | แผ่นป้ายชื่อหัวหน้า (หน่วยการเรียนรู้ / เลขมโนทัศน์ / ปฏิกิริยา) |
| `.panel` + `.face` + `.readout` + `.win` | แผงหน้าปัด: กราฟซ้าย + หน้าต่างอ่านค่าขวา |
| `.caliper` | แถบควบคุมใต้แผง (สไลเดอร์ + ปุ่ม) |
| `.sec` + `.sec-hd` + `.sec-no` + `.sec-bd` | กล่องหัวข้อหลัก (2.1, 2.2, ...) |
| `.notes` | กริดกล่องนิยามสั้น ๆ (`<b class="k">หัวข้อ</b>เนื้อหา`) |
| `.eq` | กล่องสมการกลางหน้า (เพิ่ม `.long` ถ้าสมการยาว) |
| `.frac` | เศษส่วนซ้อน — `<span class="frac"><i class="n">mol</i><i class="d">dm³·s</i></span>` |
| `.cards` | การ์ดเรียงกริด (`<article>` ข้างใน) |
| `.traps` | กล่องเทียบ ❌ เข้าใจผิด / ✅ ที่ถูกต้อง |
| `.quiz` | คำถามปรนัยตอบทันที (`data-a="index คำตอบถูก"`) |
| `.cite` + `.cite-hd` | กล่องน้ำเงิน = อ้างอิงจากหนังสือเรียนจริง |
| `.checkhd` + `.check` | กล่อง "ตรวจสอบความเข้าใจ" ระหว่างเรียน |
| `.ex` + `.ex-body` + `.ans` + `.work` | แบบฝึกหัดท้ายบท + เฉลยแบบพับได้ (`<details>`) |
| `ol.steps` | ขั้นตอนมีเลขกำกับตัวใหญ่สีชาด (**อย่าพิมพ์ "1." ซ้ำในข้อความ** เลขขึ้นอัตโนมัติ) |
| `.hookrow` | แถวภาพเกริ่นนำ + `.hookq` คำถามเปิด |
| `.fig` | ภาพประกอบพร้อมคำบรรยายใต้ภาพ |
| `.srcnote` | หมายเหตุสำหรับผู้สอน (กรอบเส้นประ) |
| `.reallife` (มโนทัศน์ 3) | กล่องตัวอย่างชีวิตจริง **แบบข้อความล้วน** (ไม่ใช้รูป) — `<span class="kick">ในชีวิตจริง</span>` ตามด้วย `.items` กริดของ `<div><b>หัว</b><p>เนื้อหา</p></div>` |
| `.waytag` (มโนทัศน์ 3) | ป้ายเล็กมุมขวาหัวข้อ บอกว่าเข้าทางกลไกไหน — `.waytag` (โคบอลต์) / `.waytag.w3` (ชาด, ทาง ③) / `.waytag.fixed` (เทา, เปลี่ยนไม่ได้) |
| `.synth` + `.synth-row` (มโนทัศน์ 3) | แถวกดเพื่อดูเฉลย ใช้ตอนสรุปท้ายหน้า — `<button class="synth-row" aria-expanded="false"><span class="f">หัวข้อ</span><span class="m" data-a="คำตอบ">กดเพื่อดู</span></button>` แล้ว JS toggle ข้อความใน `.m` |
| `.flaskfig` + `.flaskcaps` (มโนทัศน์ 3) | กรอบขวดสาธิต SVG คู่ + แคปชันเวลาใต้ภาพ 2 คอลัมน์ — ดูฟังก์ชัน `hw(y)`/`build()` ใน `03-factors.html` สำหรับวาดขวดรูปกรวยที่มีของเหลว/ฟอง/ของแข็งลอยแบบไม่ล้นขอบ (คัดลอกไปแก้ต่อได้เลยถ้าต้องใช้ขวดอีก)

## กติกาเนื้อหา

1. **หน่วยเขียนเป็นเศษส่วนซ้อนเสมอ** ใช้ `.frac` ห้ามใช้ `cm³·s⁻¹`
2. **แยกให้ชัดว่าอะไรมาจากหนังสือจริง** — ใช้ `.cite` และเขียน `.srcnote` บอกตรง ๆ ว่าข้อไหนคัดมา ข้อไหนเขียนเอง
3. **ทุกหน้าต้องมีแบบฝึกหัดพร้อมเฉลยแบบพับได้** และควรมี "ตรวจสอบความเข้าใจ" คั่นระหว่างเรียนด้วย
4. **ทุกหน้าควรมีของโต้ตอบอย่างน้อย 1 ชิ้น** ที่ทำให้เห็นมโนทัศน์หลักเคลื่อนไหวได้
5. **สมการในโจทย์ให้นักเรียนดุลเอง** ถ้าจุดประสงค์คือฝึกสัมประสิทธิ์

## วิธีสร้างภาพประกอบ (ผ่าน codex MCP)

> **⚠️ ใช้ได้กับมโนทัศน์ 1, 2 เท่านั้น** — มโนทัศน์ 3 ผู้ใช้ปฏิเสธภาพ raster ทั้งหมดแล้วขอ SVG ล้วนแทน
> (ดู "บทเรียนสำคัญจากการทำหน้านี้" ในหัวข้อมโนทัศน์ 3 ด้านบน) **อย่าเจนภาพให้มโนทัศน์ 4–6 โดยไม่ถามก่อน**

โจทย์สไตล์ที่ใช้ได้ผล — ต้องระบุครบทุกข้อนี้เพื่อให้ภาพชุดใหม่เข้ากับของเดิม:

```
Clean flat vector educational-diagram style, like a modern science textbook
figure. NOT photorealistic, NOT 3D, NOT cluttered. Thin confident linework.
Palette: background #F7F8F6, ink/linework #171D1B, cool accent #17457F
(cobalt), second accent #C0301A (vermilion) used sparingly — at most one
small element per image.
NO embedded text, words, numbers or labels baked into the image.
~4:3 ratio, 900-1200px wide.
```

เสร็จแล้ว**ต้องฝัง base64** ก่อน publish (CSP ของ Artifact บล็อกไฟล์ภายนอก):

```js
// _inline.mjs — รันแล้วลบทิ้ง
import { readFileSync, writeFileSync } from "fs";
const file = "0X-xxx.html";
let html = readFileSync(file, "utf8");
for (const name of ["img-aaa", "img-bbb"]) {
  const b64 = readFileSync(`${name}.png`).toString("base64");
  html = html.split(`src="${name}.png"`).join(`src="data:image/png;base64,${b64}"`);
}
writeFileSync(file, html, "utf8");
```

## เช็กก่อน publish ทุกครั้ง

```bash
grep -o '<div' FILE.html | wc -l; grep -o '</div>' FILE.html | wc -l
grep -o '<section' FILE.html | wc -l; grep -o '</section>' FILE.html | wc -l
grep -o '<details' FILE.html | wc -l; grep -o '</details>' FILE.html | wc -l
grep -o '<svg' FILE.html | wc -l; grep -o '</svg>' FILE.html | wc -l
```
ตัวเลขต้องเท่ากันทุกคู่

## ขั้นตอนเผยแพร่

1. ทำหน้าเสร็จ → ฝัง base64 → เช็ก tag balance
2. เปิดดูในเบราว์เซอร์จริงก่อน (อย่าเชื่อว่าโค้ดถูกแล้วจะแสดงผลถูก)
3. แก้ลิงก์ใน `index.html` (เปลี่ยน `class="soon"` เป็น `href=`)
4. `git add` → `commit` → `push`
5. GitHub Pages rebuild เองประมาณ 1 นาที

---

## แหล่งอ้างอิงในโฟลเดอร์ `สสวท book/`

คู่มือครู เคมี ม.5 เล่ม 3 (ฉบับปรับปรุง พ.ค. 66) — บทที่ 8 อัตราการเกิดปฏิกิริยาเคมี

> เลขมโนทัศน์ในตารางนี้คือเลข**ปัจจุบัน** (หลังยุบมโนทัศน์ 3 เดิมเข้า 2 แล้ว) ไม่ใช่เลขเดิมในเล่ม
> หน้าอ้างอิงบางแถวมาจาก**หนังสือเรียนตัวจริง** (ที่ครูใช้สอน) ซึ่งเลขหน้าต่างจาก PDF คู่มือครูที่ใช้ค้นเนื้อหาไว้ก่อนหน้านี้
> (เช่น แบบฝึกหัด 8.1 → 72–73, แบบฝึกหัด 8.2 → 78–80 ยืนยันจากครูโดยตรง)

| หน้า | เนื้อหา | ใช้กับมโนทัศน์ |
|---|---|---|
| 54–55 | เปิดบท (แอร์แบ็ก NaN₃) + จุดประสงค์การเรียนรู้ | 6 |
| 56–57 | ความหมายของอัตรา | 1 |
| 58–59 | กิจกรรม 8.1 (Mg + HCl ชุดกระบอกฉีดยา) | 1 |
| 60–63 | นิยามเชิงคณิตศาสตร์ + **ตาราง 8.1** (2NO₂→2NO+O₂ ข้อมูลจริง) | 1 |
| 64–65 | ตาราง 8.2 อัตราเทียบสัมประสิทธิ์ | 1 |
| 68–69 | **กราฟรูป 8.2** + วิธีหาอัตรา ณ ขณะหนึ่งด้วยเส้นสัมผัส | 1 |
| 70–71 | กิจกรรม 8.3 (ข้อมูล Zn + HCl / A→B+C) | 1 |
| 72–73 | **แบบฝึกหัด 8.1** + แนวคิดทฤษฎีการชน | 1, 2 |
| 74–75 | ทิศทางการชน (NO + NO₃) + สารเชิงซ้อนกัมมันต์ | 2 |
| 76–77 | **แผนภาพพลังงาน รูป 8.5–8.7** (Eₐ ไปกลับ, ΔH, กราฟหลายยอด) | 2 |
| 78–80 | **แบบฝึกหัด 8.2** (ยืนยันเลขหน้าจากครูโดยตรง) | 2 |
| 80–81 | **กิจกรรม 8.4** (CaCO₃ ก้อน/ผง + HCl 0.2/0.5 M) | 3 |
| 82–83 | รูป 8.8, 8.9 (อนุภาค) + **รูป 8.10 การกระจายพลังงานที่ T₁, T₂** | 2, 3 |
| 84–85 | **กิจกรรม 8.5** (H₂O₂ + KI ตัวเร่ง) | 3 |
| 86–87 | รูป 8.11 (ตัวเร่งลด Eₐ) + กิจกรรม 8.6 | 2, 3 |
| 88–89 | **แบบฝึกหัด 8.3** — เป็นแบบฝึกหัดเรื่อง**ปัจจัย** ทั้งชุด (ไม่ใช่กฎอัตรา) · ข้อ 2 มีตาราง A/B ที่มโนทัศน์ 4 ยืมมาใช้เป็นตัวเปิด | 3 (ทั้งชุด), 4 (ข้อ 2) |
| 90 | สรุปแนวคิดสำคัญประจำบท | 6 |
| 92–93 | แบบฝึกหัดท้ายบท (KClO₃, CH₃COOH + NaHCO₃) | 3, 6 |
| 97–120 | **เฉลยแบบฝึกหัด 8.1 แบบละเอียด** (สไลด์) | 1 |
